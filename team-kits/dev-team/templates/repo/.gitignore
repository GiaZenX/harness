# Generated / tooling artifacts — never commit these.
sbom.json
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
node_modules/
frontend/coverage/
frontend/dist/
*.db
.venv/
venv/
target/

# generated dashboard snapshots (churn + security-review noise; retention keeps last 20 locally)
project_memory/dashboard_history/

# append-only hook/agent event log (diagnostic, read by scripts/retro.py — never project state;
# the notify_agent_events hook writes here on every background-agent completion = git-status churn)
project_memory/.audit/

# session-local kit bookkeeping (session_status owns these; per-machine state, never project state)
.claude/kit_last_seen_version
.claude/kit_update_pending.state
.claude/.gate_pipeline_green
