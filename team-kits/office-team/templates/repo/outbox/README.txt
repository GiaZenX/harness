OUTBOX — drafts awaiting YOUR send (untracked).

The team NEVER sends, posts or publishes anything. Per-role subfolders (outbox/product-editor/,
outbox/marketing-planner/, ...) hold finished drafts (supplier queries, posts, pages). You review,
you send — then tell the manager so calendars/logs get updated.
