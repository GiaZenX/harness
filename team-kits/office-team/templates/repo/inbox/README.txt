INBOX — drop zone (untracked).

Put new documents here (invoices, receipts, product data, exports). The team files each item
into archive/ per the filing plan and logs it in project_memory/filing_log.yaml — verified by a
hook, the file must really exist. Items the team cannot classify go to archive/_unsorted/ with a
question list; nothing is ever deleted.
