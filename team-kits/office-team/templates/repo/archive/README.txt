ARCHIVE — system of record (untracked in git; the folder tree IS the Aktenplan).

Structure + naming + retention live in project_memory/filing_plan.yaml. Files are MOVED here,
never deleted, never altered/re-saved (a hook blocks shell deletes here and moves out of here).
Reorganisation only as a user-approved migration (dry-run -> OK -> move + manifest).
