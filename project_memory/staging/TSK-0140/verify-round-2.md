# Prüfbericht TSK-0140 (PR-0012, Order 3) — Runde 2 — **PASS**

Prüfer: `harness-verifier` (Opus, high), ~8 min, 318 k Token. Als Text geliefert, vom Lead hierher gelegt (2026-09-12 09:46, Uhr gelesen). Nachträge des Lead: G2 → H14/H153 werden `gesperrt` (Nutzer-Patch), G4 → H141 wird `ja` (Strom A, tools/test_parallel_streams.py), G3 → Strom A, der Volllauf ist der des Zielrunden-Auftrags und der CI.

## Prüfbericht TSK-0140 — Runde 2 (kurz) — Verdikt: **PASS**

Gemessen in frisch synchronisierter Kopie (`…/_round-scratch/TSK-0140/verify/tree`, `store`), Pilot mit frischem Kit (`pilot2` über einer Kopie des echten Stores), eigenes Rig (nur eigenes Verzeichnis, Binär-IO). Kein Vollauf, ein pytest zur Zeit.

### F1–F11 nachgemessen

| | Ergebnis |
|---|---|
| **F1** | **behoben.** `cli.py:1756-1765`: positionale Id zuerst. Prozess: `request-approval hole_exception BUG-0005` → rc 2 *„…der id goes on --batch… `--batch BUG-0005`"* (Id in der Hand); ohne beides → *„none was named"*, Verb neutral („is asked over", kein „closes"). Mutation (Prüfung zurückgetauscht) → `test_a_batch_kind_given_a_positional_id_is_sent_to_the_flag_with_that_id_in_hand` **rot**. Restrisiko, nicht blockierend: bei *beidem* nennt die Remedy nur die positionale Id und lässt die `--batch`-Liste fallen. |
| **F2** | **behoben.** Drei von sechs gelesen: H13/BUG-0105 („`_harness.decision_inputs` fragt den Interpreter über `sys.modules` … offen bleibt die DATEI-Granularität, begrenzt weil eine solche Datei nichts entscheidet, bis etwas sie importiert"), H18/BUG-0110 („Über-Verweigerung, nichts geht durch … Ausweg gemessen: Kopie als Python-Skript unter dem Scratch-Root"), H15/BUG-0107 („die Kopplung ist gewollt … Fehlrichtung ist LAUT und geschlossen: ein umbenannter Helfer lässt den Borgvorgang werfen, ein Gate, das nicht laufen kann, verweigert"). Jede Zeile begrenzt **dieses** Loch und nennt Code, den ich geprüft habe: `decision_inputs` liest wirklich `sys.modules` (`_harness.py:788`), die sechs geborgten Helfer existieren in `gate_write_scope.py`. Kein geteilter Satz mehr. |
| **F3** | **behoben.** Sechs Spalten: `hole | bug | sev | mechanism | why not closed here | what bounds it today`. |
| **F4** | **behoben und ehrlich beziffert.** Die Sieben sind alle `VERIFIED` und archiviert unter `APR-0047` (BUG-0067/0220/0249/0258/0268/0276/0279). `EVD-0322`: `kind: test`, `result: pass`, `run_scope: selection`, drei nennende Knoten. `report.stock_rollup` in Repo-Lage: **45** — genau die Zahl, die das Protokoll nennt; nach allen Ausnahme-Klicks blieben **6**, und das steht so im Protokoll (`:344-348`). |
| **F5** | **behoben** (Korrekturzeile im append-only Journal, nennt 18 vs. gemessene 17 und dass `--write` selbst `recorded 17 change(s)` meldete). |
| **F6** | **behoben.** `CLAUDE.md:183` nennt jetzt den einen Kit-Hook und warum er dort referenziert wird; gegengeprüft an `.claude/settings.json`: `gate_approval.py` steht zweimal (Pre/PostToolUse `AskUserQuestion`), der Kopfkommentar begründet es. |
| **F7** | **behoben**, soweit erreichbar: die zwei Präsens-Stellen in `test_gates.py` sind weg; `:534` (historische Behauptung) und `:1812` (datierte Messung) bleiben zu Recht. Die zwei gesperrten Dateien stehen als Patch-Abschnitte 4 und 5 mit Vorher/Nachher, und der Abschnitt „Not to change" unterscheidet Messung von Behauptung — richtig. **Rest, klein:** `.claude/hooks/_harness.py:1596` sagt weiter im Präsens „`.claude/settings.json`, which is the registration of all four gates"; gehört in denselben Patch. Und der Docstring-Satz „The count itself is no longer anywhere in the prose" (`test_gates.py:539`) wird erst wahr, **nachdem** der Nutzer patcht — ein Wort („in CLAUDE.md's prose") würde ihn heute wahr machen. |
| **F8** | **behoben.** `approvals.py:1447`: Nicht-String vor dem Falten verweigert. Prozess, vier Formen: Liste → rc 1 „not a sentence but a list", `7` → „a int", Mapping → „a dict", Leerraum → „states no limits". Test `…not_a_sentence_is_refused_before_it_becomes_text` grün. |
| **F9** | **gemessen und benannt** (`BATCH_LIMIT`-Kommentar, Zeile 189-198) mit meiner Zahl und der richtigen Folgerung („AT its bound rather than under it", und ein Überlauf verliert die Antwort nicht — der genannte Test `…stays_in_force_for_every_id_it_lists…` existiert). **Antwort auf die Frage: 10 bleibt richtig, 8 wäre die falsche Antwort auf die richtige Beobachtung** — die Grenze bindet nur für `verification` (~5,8 s/Eintrag wegen des Evidenz-Gangs), während `hole_exception` heute bei 0,35–1,0 s/Eintrag liegt (neu gemessen: 2 Einträge, Mint 2,0 s). Eine gemeinsame Konstante für zwei verschieden teure Arten ist die eigentliche Schwäche; die saubere Form wäre eine je Art aus der Messung abgeleitete Grenze — als Loch notieren, nicht die 10 senken (das vervielfacht nur die Fragen an den Nutzer). |
| **F10** | **behoben** im Sinn meines Befunds: H190 steht in der 3b-Tabelle als **ja** mit `tools/test_radar_trigger.py` und „die zweite Form (Liste beidseitig geprüft) ist baubar". *(Die alte Triage-Zeile 157 sagt weiter „RE-CUT → EXCEPTION"; sie ist der eingefrorene Runde-1-Datensatz, der Kopf von `exception-batches.md` verweist auf 3b — harmlos, aber zwei Antworten in zwei Dateien.)* |
| **F11** | **behoben und am echten Hook gemessen:** „Freigabe APR-0048 (hole_exception) für **die 2 Einträge ihrer Liste**. Erteilt von einem Menschen…" |

### Neue Befunde (beide Prosa/Tabelle, keiner blockiert den Commit)

**G1 — `project_memory/staging/TSK-0140/order-3b-candidates.md:11`: der Kopf zählt falsch.** Er sagt „**105 gaps: ja 79**, gesperrt 8, nein 18", die Tabelle darunter trägt **121 Zeilen: ja 95, gesperrt 8, nein 18** (geparst; deine eigene Nachricht nennt 121/95). Dieselbe Klasse wie F5: eine Handzählung neben einer generierten Tabelle. Minimalfix: `make_order_3b.py` schreibt die Kopfzahlen aus den Zeilen, die es gerade geschrieben hat.

**G2 — dieselbe Datei: drei `ja`-Zeilen nennen eine Datei, die jede Rolle hier nicht schreiben darf.** `H14/BUG-0106 → .claude/hooks/gate_commit_evidence.py`, `H153/BUG-0235 → .claude/hooks/gate_test_scope.py` (beide `forbidden_scope: .claude/hooks/gate_*.py`), dazu `H10/BUG-0102 → ".claude/hooks/ (Mutationslauf)"` — bei H10 vertretbar, wenn die Lieferung ein Test ist, bei den anderen zweien nicht: nach der Definition im Kopf derselben Datei („gesperrt … `.claude/hooks/**`") gehören sie in die gesperrte Klasse und in den Nutzer-Patch. Ein Auftrag 3b, der sie als `ja` vergibt, gibt einem Umsetzer Arbeit, die er nicht tun darf — eine Runde Verlust. Umgekehrt korrekt: `H41/H45/H161 → .claude/hooks/test_gates.py` sind zu Recht `ja` (ausdrücklich im `allowed_scope`), nur sagt die Klassendefinition im Kopf das nicht. Minimalfix: die Klasse aus dem Pfad gegen dieselbe verbotene Menge ableiten, die die Items tragen, statt sie zu tippen — und den Kopfsatz um die Ausnahme `test_gates.py` ergänzen.

**G3 — `tools/test_approvals_dispatch.py::test_the_card_of_a_list_bound_approval_counts_what_it_binds` kann den Zähler nicht falsch sehen.** Gemessen: Mutation `len(listed) + 10` → die Karte sagt „die **12** Einträge" für eine Liste von 2, der Test bleibt **grün** (`"2" in card` trifft in „12", „keinen Vorgang" fehlt weiter). Severity: niedrig, aber es ist die teurere Sorte — ein benannter Test, der seine Eigenschaft nicht misst. Minimalfix: auf das exakte Fragment prüfen, `"die %d Einträge" % len(holes)`.

**G4 — Konsistenzfrage, kein Fehler: H141/BUG-0224 steht als `nein` (Weltwissen), obwohl derselbe Teil-Fix wie bei H190 in diesem Repo läge.** `tools/test_parallel_streams.py:622-626` führt fünf der blinden Schreibweisen bereits als eigene Testzeilen mit `False`; die Aufzählung um sie zu erweitern ist in-repo machbar. Die Klasse („ist dieser Satz eine Taktangabe") bleibt Weltwissen — aber H190 wurde mit genau diesem Argument nach `ja` verschoben. Entweder beide `nein` oder beide `ja`; die Entscheidung ist deine.

### Stichproben der 3b-Tabelle

* **fünf `ja`** (H190, H161, H118, H14, H77): bei H190 (`tools/test_radar_trigger.py`), H161 (`.claude/hooks/test_gates.py`), H118 (`team-kits/office-team/templates/repo/scripts/`) und H77 (`team-kits/*/constitution/AGENTS.md`) liegt der Fix wirklich dort; **H14 ist falsch klassifiziert** (G2).
* **fünf `nein`** (H141, H131, H36, H49, H169): vier tragen einen gemessenen Provider-/Weltgrund — H36 CPython (ein C-Aufruf gibt den Interpreter nicht zurück, steht mit Messung in `CLAUDE.md`), H49 Claude Code (`stop_hook_active`, zweite Blockade wäre eine Schleife), H169 Claude Code (Agent-Werkzeug hat keinen Effort-Parameter, gemessen 2026-09-05 mit einem verweigernden Hook, der `tool_input` dumpt), H131 amtlicher Vordruck liegt nicht vor. **Kein verkappter In-Repo-Fix darunter außer H141** (G4).

### Weiteres, gemessen

Sieben Tests der Ausnahme-Route grün (7 passed); Stempel **2026.09.12-2 ×3**, `bump_kit_version.py` → „unchanged" ×3; `ruff check tools team-kits` → *All checks passed!*; `tools/validate.py` → *all structural checks passed*; `generate-index` erzeugt nur einen neuen `generated_at` → **Index = Store**; Spiegel byte-identisch (`gate_approval.py` und `_routine.py` je ein md5 über alle drei Kits); `approval_card` wird an allen vier Aufrufstellen mit `state` gerufen (drei Kit-Hooks + `sdk_approval.py:79`).

**Nicht gemessen:** kein Vollauf (der gehört vor den Commit, und er hat nach dieser Nacharbeit an `approvals.py`/`cli.py`/`gate_approval.py`×3/`sdk_approval.py` **noch nicht** stattgefunden); ob der echte Client eine Options-Beschreibung von jetzt ~2.800 Zeichen (10 Einträge mit den längeren Bounds) wortgleich zurückspiegelt — schlägt er fehl, mintet nichts, also die sichere Richtung; die 12 übrigen Ausnahme-Batches als Prozess; die 121 Zeilen der 3b-Tabelle jenseits der zehn Stichproben.

### Verdikt

**PASS.** Alle elf Befunde der ersten Runde sind behoben oder als benannter, patchbarer Rest geführt; jede Behauptung, die ich nachgemessen habe, hält. G1–G3 sind nicht blockierend: G1/G2 betreffen die **Eingabe für Auftrag 3b** und sollten vor dem Schneiden der 3b-Aufträge korrigiert werden (sonst bekommt ein Umsetzer gesperrte Arbeit), G3 ist ein Test, der seine Eigenschaft nicht misst.

**Ist der Baum commit-reif?** Ja bis auf zwei Dinge, die dem Lead gehören: der **eine volle Lauf nach der letzten Nacharbeit** (Lieferkriterium, DEC-0050 — er fehlt, die Nacharbeit hat Kernel-Code geändert) und die **Commit-Evidenz mit dem Diff-Hash** für Gate 3. Zustand, Stempel, Spiegel, Index, Linter und Validator sind sauber, die sieben Verifikationen stehen im Zustand.