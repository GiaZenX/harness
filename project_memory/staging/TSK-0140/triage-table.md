# TSK-0140 triage: every hole, its mechanism class, its bound, its verdict

Generated by `_round-scratch/TSK-0140/make_triage.py` from the STORE (`project_memory/bugs/active/*.yaml`, the `hole_number` field) joined with the verdict map. The generator refuses when store and map disagree in either direction, so a hole captured after this table was written cannot hide in it.

**Counts.** 128 holes: CLOSE 22, EXCEPTION 105, ALREADY-CLOSED 1.

| class | CLOSE | EXCEPTION | ALREADY-CLOSED | total |
|---|---|---|---|---|
| OVERREF -- over-refusals of an enforcement gate | 2 | 10 | 0 | 12 |
| ENUM -- enumerations awaiting a definition | 1 | 7 | 0 | 8 |
| PROV -- provider and platform limits | 0 | 12 | 0 | 12 |
| INSTR -- limits of the measuring instrument | 11 | 23 | 0 | 34 |
| DESIGN -- bounds taken by design | 8 | 53 | 1 | 62 |

**Measured before the table** (`_round-scratch/TSK-0140/naming_scan.py`, reader `kernel.naming_tests.nodes_naming`, the DEC-0100 (3) declared place): of 128 holes exactly ONE is named by a test today (H184/BUG-0266). A CLOSE therefore always means a new test that names the hole's bug id, never a re-run of something standing.

## CLOSE (22)

| hole | bug | sev | class | where the fix lives / why not closable here | bound today (`limits`) |
|---|---|---|---|---|---|
| H47 | BUG-0139 | medium | OVERREF | team-kits/*/hooks/gate_write_scope.py -- pull the line-assignment map into the SHARED target reader gate 1 borrows | 'dieselbe Klasse wie H46, aber die **Variablen**-Variante: Gate 1 leiht den Ziel-Leser des Kits, nicht dessen Zeilen-Zuweisungskarte, also ist `F=team-kits/kernel/state.py; echo x > $F` am … |
| H57 | BUG-0149 | medium | OVERREF | team-kits/*/hooks/_compat.py + gate_ledger_valid -- an INTERPRETER heredoc body must stay visible | 'ein quotiertes Heredoc an einen INTERPRETER (`python <<''EOF''`) ist vor `gate_ledger_valid` unsichtbar, seit es `_compat.literal_heredoc_free` übernimmt — der Schreibzugriff im Körper kan… |
| H66 | BUG-0158 | medium | INSTR | team-kits/*/hooks/_compat.py -- build the PowerShell backtick reading the docstring promises | '`_compat.shell_readings` sagt „jede Lesart, die eine gewöhnliche Shell dem Text geben könnte" zu und liefert nur die POSIX-Backslash-Lesart; PowerShells Backtick als Fluchtzeichen wird von… |
| H94 | BUG-0186 | medium | DESIGN | team-kits/research-team/constitution/AGENTS.md -- name `freeze-report` in the section that owes the file | der Schreibweg existiert und ist gemessen (`freeze-report`, `staging.freeze_report`); begrenzt wird der Rest dadurch, dass §6/§17 den Befehl nicht NENNEN — ein Report-Writer muss ihn heute … |
| H111 | BUG-0195 | medium | DESIGN | the routine kind EXISTS since generation 6 -- a test naming BUG-0195 measures the producer | 'die Freigabe-Art, auf der die Auditor-Routine laut aller drei Verfassungen reitet (`routine`, ersatzweise `analysis`), hat in keinem Kit einen Erzeuger: `request-approval` bietet neun Arte… |
| H123 | BUG-0207 | low | DESIGN | team-kits/office-team/hooks/guard_fs_tripwire.py -- a copier flag that deletes in the TARGET (/MIR, --delete) | 'die Flaggen-Form ist größtenteils GESCHLOSSEN: seit TSK-0116 liest die Regel ein zerstörendes Wort an JEDER Stelle einer Aufrufung, also auch in einer Flagge — `find archive -name x.pdf -d… |
| H130 | BUG-0213 | low | DESIGN | team-kits/kernel/approvals.py -- the empty retention the template allows must be askable | 'die zweite ehrliche Form einer Aufbewahrung — `retention: null` für ein Fach ohne zählbare Frist, die die Vorlage ausdrücklich erlaubt — ist über `add-filing-rule` NICHT erreichbar: `appro… |
| H137 | BUG-0220 | low | INSTR | team-kits/*/hooks/_routine.py -- the module docstring claims a cadence no constitution states | 'der Modul-Docstring von `hooks/_routine.py` sagt, jede Verfassung reite die Auditor-Rolle auf einem wöchentlichen Rhythmus — das stimmt für keine der drei mehr: dev und research nennen bei… |
| H154 | BUG-0236 | medium | DESIGN | team-kits/kernel/state.py -- the second class: _iter_every_stored_item swallows a read error, so a broken hole item vanishes from all three judges | 'die Migrationstuer `state.capture_migrated_hole` schreibt einen ENDZUSTAND -- `VERIFIED` darunter -- ohne die bestaetigende Evidenz, die `state._assert_confirmed` auf der gelaufenen Kante … |
| H161 | BUG-0243 | medium | INSTR | .claude/hooks/test_gates.py -- build the line OUTSIDE the measured span | '`.claude/hooks/test_gates.py::test_gate1_answers_before_its_registration_however_long_the_line_takes_to_read` baut seine Zeile INNERHALB der Spanne, die es gegen die registrierte Frist hae… |
| H164 | BUG-0246 | medium | INSTR | tools/migrate_holes.py -- a MODULE name satisfies the test-citation shape | '`tools/migrate_holes.cited_tests` liest jede Backtick-Spanne als Testzitat, deren letztes Glied mit `test_` beginnt — ein MODULname erfuellt das. Gemessen in einer `.git`-losen Kopie des g… |
| H167 | BUG-0249 | medium | DESIGN | team-kits/kernel/report.py -- generate_session_brief names its fields one by one | 'The values are not lost, only unshown: `create_lease` writes them on the lease, the dispatch header carries them into the specialist''s prompt, the task item stores them, and `ladder <TSK-… |
| H168 | BUG-0250 | medium | DESIGN | team-kits/*/skills/project-manager/SKILL.md + project_config templates + session_status + scaffold | 'The tripwire that names this set is shipped and green today, so the contradiction cannot spread quietly to a fourth file or survive its own repair unnoticed; the constitution -- the text a… |
| H176 | BUG-0258 | medium | INSTR | tools/test_repo_hygiene.py -- the reader was replaced by the store-reading one; it needs to NAME the hole | 'What still holds while this is open: the hole list''s OTHER half is unaffected -- the `### L<n>` entries and their overview rows are untouched, and every pointer INTO the document from shi… |
| H180 | BUG-0262 | low | INSTR | tools/test_hooks_v2.py -- a host-load measurement must SKIP, not fail (the BUG-0033 route) | 'What takes the place of it: the failure message NAMES the two measurements and says in its own words that the difference is the host, so a reader who sees it is not misled -- the cost is a… |
| H181 | BUG-0263 | medium | INSTR | tools/test_repo_hygiene.py -- _CODE_SPAN_RX pairs backticks across a fence | 'What still holds meanwhile: the check is not blind, it is PARTIALLY blind -- 486 of 525 citations are still judged, and every file with no fence at all (most shipped kit .py and .md role t… |
| H185 | BUG-0268 | low | INSTR | CLAUDE.md -- 'die vier Gates', five are registered (CLAUDE.md is in THIS order's scope) | 'What takes the place of it: every refusal gate 5 prints carries its own remedy and names DEC-0050/FR-0086, so a reader who meets it is not left without the reason -- only the overview in C… |
| H191 | BUG-0275 | low | INSTR | tools/test_review_procedure.py -- the lead-in branch accepts the two axes a bullet apart | 'Nothing shipped is misread today: all three constitutions satisfy the strict sentence branch, and a text that drops the effort rule while keeping the word in the lead-in is a deliberate ac… |
| H192 | BUG-0276 | medium | INSTR | tools/ -- nothing measures the shadowing of a team-kits module name | 'What takes the place of the protection today: the collision is LOUD where it has been measured -- validate.py dies in its import with the file name in the traceback, so a round that causes… |
| H193 | BUG-0277 | low | DESIGN | team-kits/scaffold_team.sh + .ps1 -- warn-and-continue where the recorder is missing | 'A project installed this way runs with `hook_trust: unverified` (doctor names it) rather than with a forged trust: gate_dispatch refuses spawns only when trust is WITHDRAWN, so nothing blo… |
| H194 | BUG-0278 | low | ENUM | team-kits/kernel/dispatch.py -- the negation and word lists are English-shaped | 'Bounded by its direction: every miss keeps the order on the opus default -- nothing is granted that should not be. What it costs: a German-writing PM cannot reach sonnet with the repo''s o… |
| H195 | BUG-0279 | medium | INSTR | tools/close_measured_pass.py -- the declared-place reader EXISTS (kernel/naming_tests.py); it needs its naming test | 'Bounded today by the lead''s hand: the verifier read all 14 single-node lines and the lead removed BUG-0044 and BUG-0050 from the batches before asking (2026-09-11 18:45); multi-node bugs … |

## ALREADY-CLOSED (1)

| hole | bug | sev | class | where the fix lives / why not closable here | bound today (`limits`) |
|---|---|---|---|---|---|
| H184 | BUG-0266 | medium | DESIGN | the routine kind and its read-only route exist; two tests NAME the bug | 'The reminder is real and works, so the run is never silently forgotten: every session start of a kit project names the owed run, and the notice says in its own words that it only proposes.… |

## EXCEPTION (105)

| hole | bug | sev | class | where the fix lives / why not closable here | bound today (`limits`) |
|---|---|---|---|---|---|
| H10 | BUG-0102 | low | INSTR | an exhaustive mutation run over .claude/hooks/ is the unasked question, not a patch | die beiden gefundenen Hälften sind geschlossen und je durch einen roten Test gedeckt; für die dritte gibt es keinen Ersatz, sondern eine ungestellte Frage — ein erschöpfender Mutationslauf … |
| H13 | BUG-0105 | low | OVERREF | .claude/hooks/_harness.py (decision_inputs) -- forbidden_scope | jeweils dort benannt; H18/H19/H23 sind Über-Verweigerungen, also Reibung statt Loch, und H13/H14/H15 nennen in ihrem Eintrag, was an der Stelle steht |
| H14 | BUG-0106 | low | DESIGN | the remedy gate 3 prints must be runnable by the lead | jeweils dort benannt; H18/H19/H23 sind Über-Verweigerungen, also Reibung statt Loch, und H13/H14/H15 nennen in ihrem Eintrag, was an der Stelle steht |
| H15 | BUG-0107 | low | DESIGN | deliberate: one answer to 'is this stage read-only', not two | jeweils dort benannt; H18/H19/H23 sind Über-Verweigerungen, also Reibung statt Loch, und H13/H14/H15 nennen in ihrem Eintrag, was an der Stelle steht |
| H18 | BUG-0110 | low | OVERREF | .claude/hooks/_harness.py -- forbidden_scope | jeweils dort benannt; H18/H19/H23 sind Über-Verweigerungen, also Reibung statt Loch, und H13/H14/H15 nennen in ihrem Eintrag, was an der Stelle steht |
| H19 | BUG-0111 | low | OVERREF | .claude/hooks/_harness.py -- forbidden_scope | jeweils dort benannt; H18/H19/H23 sind Über-Verweigerungen, also Reibung statt Loch, und H13/H14/H15 nennen in ihrem Eintrag, was an der Stelle steht |
| H23 | BUG-0115 | low | OVERREF | .claude/hooks/_harness.py -- forbidden_scope | jeweils dort benannt; H18/H19/H23 sind Über-Verweigerungen, also Reibung statt Loch, und H13/H14/H15 nennen in ihrem Eintrag, was an der Stelle steht |
| H36 | BUG-0128 | low | PROV | CPython does not release the interpreter inside one C call | 'die Grenze ist gemessen und liegt außerhalb des Gates: die Zeilenlänge, ab der die nicht unterbrechbare Stelle die Frist reißt, kann auf diesem Host keine Shell mehr gestartet bekommen. De… |
| H41 | BUG-0133 | low | INSTR | four measured limits of the pointer guard, no chain | vier gemessene Grenzen des Zeiger-Wächters aus TSK-0009, je im Eintrag; keine berührt eine Gate-Entscheidung. Der lebende Bestand ist in den ersten drei Richtungen leer; die vierte (Zeiger … |
| H44 | BUG-0136 | low | DESIGN | four measured limits of the amendment derivation, no chain | 'vier gemessene Grenzen der Amendment-Ableitung aus TSK-0062, je im Eintrag: die Kriterien eines ANGEWENDETEN Änderungsantrags zählen nicht mehr (Über-Verweigerung, per Test festgehalten); … |
| H45 | BUG-0137 | low | INSTR | two measured limits of the arbiter hardening | 'zwei gemessene Grenzen der Arbiter-Härtung aus TSK-0063, je im Eintrag: die Sitzungswache verlangt die Seh-Eigenschaft auch von shell-freien Registrierungs-Prüfungen (fail-closed, auf dies… |
| H48 | BUG-0140 | medium | DESIGN | deliberate fail-soft: the display freezes, the state write succeeds | 'keine Angriffskette auf Zustand oder Durchsetzung: eingefroren ist nur die Anzeige. An die Stelle des Schutzes tritt die bedingte Frischezusage an allen fünf Prosastellen plus der Zeitstem… |
| H49 | BUG-0141 | medium | PROV | stop_hook_active: blocking twice is an endless loop | 'an die Stelle des Schutzes treten drei Begrenzungen: die erste Verletzung wird blockiert und präzise angeleitet; der Durchlass ist zustandsgenau protokolliert (`gave_up` vs `retry_delivere… |
| H50 | BUG-0142 | medium | PROV | the provider never delivers the SubagentStop | 'Preis des F1-Fixes: ein gebundenes Kind, dessen `SubagentStop` nie ankommt, ist nach dem TTL-Sweep für den Untätigkeits-Melder unsichtbar. An die Stelle des Schutzes tritt `sweep_orphaned_… |
| H51 | BUG-0143 | medium | DESIGN | at most one refusal per finding, or it loops | die Verweigerung des Zugendes kommt höchstens einmal je Befund; danach tragen `idle_reported` auf dem Item, die Board-Karte und der Verfassungstext — die Audit-Zeile jedes weiteren Stops er… |
| H52 | BUG-0144 | medium | DESIGN | refuse rather than guess an owner | 'ohne `agent_id` wird verweigert statt geraten; ein Zombie-Dispatch hält damit rollengleiche id-lose Zuordnungen dauerhaft still. Begrenzung: der repo-eigene Messwert sagt, dass `SubagentSt… |
| H53 | BUG-0145 | medium | PROV | the lifetime of the provider key is not measurable here | die Lebensdauer von `stop_hook_active` ist ungemessen; die Fehlrichtung ist stumm, nie schleifend, und kein ausgelieferter Text behauptet mehr als „at most once per finding" |
| H54 | BUG-0146 | medium | DESIGN | the finding is record-true; gate layer 3 refuses the unbound child anyway | ein ungebunden LAUFENDES Kind wird nach Fensterablauf als „nichts hat diesen Lauf je verfolgt" gemeldet — die Aussage ist datensatz-wahr und die Meldung gewollt; begrenzt, weil Gate-Schicht… |
| H55 | BUG-0147 | medium | DESIGN | old stock before kit 2026.08.14-9 has no approval kind to mint | 'die Update-Brücke im Alt-Bestand läuft auf ein gesprochenes Ja statt einer geminteten Freigabe, und auch ein SUBAGENT erreicht sie (Alt-Gate: bridge rc 0, scaffold rc 2) — die Brücke liest… |
| H56 | BUG-0148 | medium | DESIGN | a killed run is not undone; the second run lifts cleanly | ein mitten im Kopieren abgebrochener Brückenlauf lässt ein gemischtes Bündel ohne Rücknahme stehen (kernel_files 17→22, Stempel alt); der zweite Lauf hebt sauber (rc 0), `project_memory` un… |
| H58 | BUG-0150 | medium | DESIGN | a rule on a never-walked edge, and it would break the V1 import | 'die Kante `TSK DONE → VALIDATED` fordert keine Evidence (`state.CONFIRMING_EVIDENCE` kennt nur BUG); die Einzeiler-Regel dort säße auf einer in drei Populationen nie begangenen Kante und m… |
| H59 | BUG-0151 | medium | DESIGN | nothing drives a solo project into phases 6-9; the emptiness is said | 'nichts treibt ein Projekt in die Phasen 6–9 — der Merge ist der einzige gebaute Forderer der evidence-Schublade, und ein Solo-Projekt auf `main` merged nie (Pilot 3: 11 DONE/0 Evidence; Pi… |
| H60 | BUG-0152 | medium | DESIGN | an empty plan refuses the first document anyway | '`document_sources` erzwingt nichts: kein Gate liest die Liste, ein nicht begangenes Interview sieht aus wie „nichts zu melden" — aber ein leerer Plan verweigert das erste Dokument ohnehin … |
| H61 | BUG-0153 | medium | DESIGN | porting _harness.Deadline into the kit hooks is a build, not a fix | 'kein Kit-Hook merkt selbst, dass sein Fenster abläuft — jede Grenze in einem Kit-Hook ist ein Versprechen des Hooks, keine Durchsetzung; die Konstruktion, die schließt, existiert im Harnes… |
| H62 | BUG-0154 | medium | OVERREF | the one-line candidate was measured and rejected (it reopens BUG-0064's neighbour) | 'die Köder-Prüfung des Ledger-Gates (`_DECOY_VALIDATOR_RX`) urteilt noch über das ganze Segment, während dieselbe Frage seit TSK-0083 sonst je Stufe gestellt wird — ehrliche Argument-Prosa … |
| H64 | BUG-0156 | medium | OVERREF | closing means knowing which `>` the shell executes | '`gate_ledger_valid` liest seit dem F3-Fix JEDES `>` eines Segments als Umleitung — auch in quotierter Argument-Prosa, auch in den Argumenten eines verbürgten Laufs, weil die Redirect-Prüfu… |
| H65 | BUG-0157 | medium | INSTR | a PreToolUse hook has no shell state | ein Zielwort, dessen Pfad die Shell erst durch Expansion **herstellt oder beendet** (`$f`, `${…}`, `${X:-…}`, Glob), steht so nicht im Text — dieser Leser sieht nur die Schreibweise, nicht … |
| H67 | BUG-0159 | medium | DESIGN | the decoy question is only asked where the line already blocks | 'die Köder- und Geschwisterprüfung des Ledger-Gates wird **nur befragt, wenn dieselbe Zeile eine blockierte Operation trägt**. Ein Lauf eines unbewachten Zwillings ohne Commit und ohne Ledg… |
| H68 | BUG-0160 | medium | OVERREF | the obvious fix was measured wrong (it reopens BUG-0065) | 'zwei Schreibweisen, die das Ledger-Gate verweigert, obwohl sie nichts schreiben: ein **handgetippter Backslash** im Validatorpfad (`python scripts\ledger_add.py --validate ledger/2026.csv`… |
| H69 | BUG-0161 | medium | OVERREF | .claude/hooks/_harness.py does not go through _kernel.payload -- forbidden_scope | 'die Gates dieses Repos erben die CR-Härtung der Kits nur zur Hälfte: der Trenner-Teil sitzt in `_compat` und greift mit (rc 0 → rc 2), die **Verschweißung** unter dem Bash-Werkzeug hängt a… |
| H70 | BUG-0162 | low | INSTR | the completeness wire asks for PATTERNS, so a pattern-free exemption is invisible | 'der Vollständigkeits-Draht des Ledger-Gates fragt nach **Mustern** (zwei Leser, Vereinigung), also sieht er eine vierte Ausnahme nicht, die **gar kein** `re.Pattern` befragt — gemessen von… |
| H71 | BUG-0163 | low | INSTR | four measured limits of the merge-backlog reader | 'vier gemessene Grenzen des Lesers der Merge-Rückstandsliste, je im Eintrag: ein Eintrag, den `open()` mit etwas anderem als `OSError` ablehnt, bricht `update-kit` nach erfolgreichem Instal… |
| H72 | BUG-0164 | low | DESIGN | measured limits of the four-eyes wall; tightening is a user decision | 'gemessene Grenzen der Vier-Augen-Wand, je im Eintrag: die Aufräum-Ausnahme vergleicht Vorlage+Dateiname statt Ort (ein Platzhalterwechsel läuft ohne Lesung, K7/K8 — Verschärfung wäre eine … |
| H73 | BUG-0165 | low | INSTR | four measured limits of the decision-first guard | 'vier gemessene Grenzen der Entscheidungs-zuerst-Runde, je im Eintrag: jede Id in einem Begrenzer, der länger ist als sie selbst, bleibt ungeprüft (16 der 22 gequoteten Spannen sind Text, d… |
| H74 | BUG-0166 | low | INSTR | measured limits of the faster gate suite; host load is not a code fact | 'gemessene Grenzen der schnelleren Gate-Suite, je im Eintrag: kein Schutz gegen einen zweiten Läufer (und die Suite drückt als Nachbar selbst stärker, 10→26); der Faktor 11 war Wirtslast, d… |
| H75 | BUG-0167 | low | DESIGN | the money guard is arithmetic, not semantics (FR-0065 carries the second reading) | 'gemessene Grenzen des E-Rechnungs-Geldpfads, je im Eintrag: der Wächter ist Arithmetik, nicht Semantik (`FR-0065` trägt die zweite Lesung); BR-CO-14 ungeprüft (in sich stimmiger, positions… |
| H76 | BUG-0168 | low | DESIGN | measured limits of the document write route | 'gemessene Grenzen des neuen Dokument-Schreibwegs, je im Eintrag: der Listeneintrag-Prosakanal (einziger Kanal ohne Wortlaut in der Karte, dreifach begrenzt); eine Freigabe deckt binnen ihr… |
| H77 | BUG-0169 | low | INSTR | no gate reaches free text | 'gemessene Grenzen der Wertsprache-Regel, je im Eintrag: kein Gate erzwingt sie (Freitext); der Prosa-Test liest Anker und Ehrlichkeit, nicht Richtung und nicht Vokabelfreiheit (Umkehrung u… |
| H78 | BUG-0170 | medium | PROV | a client start mode that loads no project settings -- nothing buildable from inside | 'ein Client-Startmodus, der die Projekteinstellungen nicht lädt, entfernt alle vier Registrierungsflächen des Kits, während die Dateiwerkzeuge im Projekt schreibfähig bleiben — drei Mitglie… |
| H79 | BUG-0171 | low | INSTR | the derivation reads names, not intentions | 'gemessene Grenzen der Besitz-Ableitung für die Dokument-Schreibroute, je im Eintrag: eine erfundene Zweitdatei ohne `staging/`-Präfix sieht der Leser nicht (die Ableitung liest Namen, nich… |
| H81 | BUG-0173 | low | INSTR | _invoked_scripts also carries doctor and the bundle trust check -- a round of its own with its own reds | 'der Mint-Leser irrt in beide Richtungen, beide gemessen: eine Registrierung mit quotiertem Pfad, der ein **Leerzeichen** enthält, zerlegt `_invoked_scripts` nicht → `False` + Warnung, obwo… |
| H82 | BUG-0174 | low | DESIGN | seven named limits of the design sighting loop, each with the measured price of its alternative | sieben benannte Grenzen der internen Sicht-Schleife für Design-Entwürfe, je mit gemessenem Preis der Alternative; der Kern — ein gestagter Entwurf erreicht den Nutzer nicht ohne Renderdaten… |
| H84 | BUG-0176 | low | DESIGN | the reference derivation informs, it does not grant | 'die Ableitung der Referenz-Skills erzwingt nichts: der Fehler ist vorhergesagt und nicht gemessen, der Dispatch-Header gewährt nichts, die Fluchttür ist Prosa, die zweite Stolperdraht-Rich… |
| H85 | BUG-0177 | low | INSTR | no test of this suite goes to the network | die Herkunftsprüfung sieht keine UNMARKIERTE Textänderung (kein Test dieser Suite geht ins Netz; ersetzt durch `source_commit`/`source_blob_sha1` im Frontmatter), `NOTICES.md` reist nicht i… |
| H86 | BUG-0178 | low | ENUM | closing means a SECOND definition of 'V1 record' | 'drei Grenzen EINES V1-Erkenners, den `migrate` und `report` schon benutzen — ein Speicher außerhalb des Zustandsverzeichnisses, ein umbenannter darin, einer im Kernelbereich; sie zu schlie… |
| H88 | BUG-0180 | low | DESIGN | older backups carry no RESTORE_SET and are refused with a reason | 'der Rollback ist byte-gleich nur über die aufgezeichnete Menge; ältere Sicherungen tragen keine und werden mit Grund verweigert statt geraten, und die Rückstandsliste des NEUEN Kits überle… |
| H89 | BUG-0181 | low | PROV | without git the baseline cannot be drawn | 'ohne `git` kann die Vier-Augen-Buchung Alt- von Neuzeilen nicht unterscheiden und TRITT ZURÜCK statt zu verweigern; die Gegenrichtung säße auf genau dem Commit, der die Zeilen zu Altzeilen… |
| H90 | BUG-0182 | low | DESIGN | belongs in the validator's duplicate rule, not in this gate | zwei identische Buchungen EINES Belegs teilen sich ein Lesepaar, solange keine `invoice_no` gesetzt ist; mit ihr greift `ledger_add.validate_cross`. Gehört in die Duplikatsregel des Validat… |
| H91 | BUG-0183 | low | DESIGN | the tree IS the plan; plan-vs-disk is the auditor's check | der gerenderte Aktenplan-Baum zeigt den PLAN und nicht die Platte — er beschreibt die Form mit `<year>`-Platzhaltern. Der Abgleich Plan↔Platte ist die Prüfung des `project-auditor` |
| H106 | BUG-0190 | low | DESIGN | no field and no hook counts runs; the role text carries it | 'der Umfang eines QS-Laufs ist Prosa: kein Feld im Evidence-Datensatz und kein Hook zählt, ob die Suite einmal oder zehnmal lief. Begrenzt durch den Rollentext — Beschreibung und Skill, bei… |
| H107 | BUG-0191 | low | DESIGN | a decision item is free text | 'der Design-Brief trennt ZIEL von SCHREIBWEISE nur als Prosa: eine Prozessregel, die in den Brief gerät, fängt nichts, weil das Decision-Item Freitext ist. Begrenzt durch die beiden Enden —… |
| H108 | BUG-0192 | medium | DESIGN | an explanation cannot be forced onto an immutable type after the fact (DEC-0061) | 'eine Evidenz, die GAR KEINEN Laufumfang erklärt, zählt weiter wie bisher — die Erklärungspflicht lässt sich auf einem unveränderlichen Typ nicht nachträglich erzwingen. Begrenzt: neue Evid… |
| H109 | BUG-0193 | medium | INSTR | the kernel parses, it does not run | '„auflösbar" heißt für den Kernel: die Datei ist da und definiert den Namen — GEPARST, nicht gefahren. Ein Test, der existiert und übersprungen wird (`skip`, ein Marker, eine Runner-Konfigu… |
| H110 | BUG-0194 | medium | DESIGN | fail-closed here forbids every non-Python project a merge | 'einen Check, dessen Datei der Kernel nicht parsen kann (ein Testfile in einer anderen Sprache), beantwortet er mit UNENTSCHIEDEN: eine Warnung, kein Merge-Blocker. Fail-closed wäre hier ei… |
| H112 | BUG-0196 | low | INSTR | the run record is a by-product of the event log | 'der Laufdatensatz der Routine ist ein Nebenprodukt des Ereignis-Logs und sagt „ein Subagent dieser Rolle hat aufgehört", nicht „ein Audit ist gelaufen". Zwei Grenzen: eine rotierte Log-Gen… |
| H113 | BUG-0197 | low | DESIGN | a 'done' would be canonical state, so kernel -- over-reporting, never silence | 'das Fristenregister kennt kein „erledigt": nichts im Kit hält fest, dass eine Voranmeldung abgegeben oder ein Aufbewahrungsjahr geprüft wurde, also steht ein Eintrag, bis seine QUELLE sich… |
| H114 | BUG-0198 | medium | PROV | /cd swaps the registration, not ${CLAUDE_PROJECT_DIR} -- client behaviour | 'nach einem Verzeichniswechsel läuft die Registrierung des Ziels, aber die Hook-DATEIEN des Startorts (`${CLAUDE_PROJECT_DIR}` bleibt stehen, und genau so buchstabieren die Kits ihre Komman… |
| H115 | BUG-0199 | medium | PROV | client behaviour: the role list does not travel | 'ein Verzeichniswechsel bringt Subagenten und `agent:`-Bindung des Ziels NICHT mit, obwohl der Changelog „agents" nennt — gemessen am Task-Fehler, an der Gegenprobe mit Neustart und am Wech… |
| H117 | BUG-0201 | low | DESIGN | the ledger has no kernel writer, so nothing can start the generator | nichts startet den Generator nach einer Buchung; der Datenstand im Kopf trägt das **nicht**, weil er nur das jüngste Ledgerdatum ist (ein Nachtrag lässt ihn byte-identisch, gemessen). Begre… |
| H118 | BUG-0202 | low | DESIGN | the page is a pure function of the data and says so | 'die Seite sagt beides selbst: „gerechnet beim Öffnen der Seite aus der Uhr dieses Rechners" und woher die 30 Tage kommen (§ 286 Abs. 3 BGB); ohne Skript bleibt der Strich stehen statt eine… |
| H119 | BUG-0203 | low | DESIGN | DEC-0056: no hardening target | 'zwei Folgen einer Wurzel: (1) `_BLOCKED_SCRIPT_RX` kennt den Generator nicht, er läuft gegen einen ungültigen Ledger — begrenzt dadurch, dass er selbst validiert und den Befund wörtlich in… |
| H122 | BUG-0206 | low | INSTR | the corpus is what git carries; the blind field is empty today | 'der Melder über ungelesene Prosa unter `docs/` fragt, was **git trägt** (`test_repo_hygiene._carried_files`, `git ls-files -c -o --exclude-standard`): eine Nennung in einer IGNORIERTEN Dat… |
| H124 | BUG-0208 | low | DESIGN | the business timezone is in no field of the kit | 'die Fristenmeldung liest die Uhr der lokalen Maschine einmal je Sitzungsstart (`_duties.briefing`, `datetime.date.today()`): eine Sitzung über Mitternacht behält die Antwort von gestern, u… |
| H126 | BUG-0210 | low | DESIGN | the import graph forbids the third reader calling the first | 'die Ablaufregel für eine offene Freigabe-Anfrage steht DREIMAL: in `approvals.open_requests` (je Datei über `pending_request`), in `report.generate_session_brief` und seit TSK-0115 in `boa… |
| H127 | BUG-0211 | medium | DESIGN | generated/ is ignored in every kit; the next write overwrites | 'eine Handänderung an einem erzeugten Diagramm sieht zwischen zwei Zustandsschreibvorgängen niemand: `plan_diagram.is_pristine` unterscheidet `pristine`/`hand-edited`/`stale` und wird von k… |
| H129 | BUG-0212 | low | ENUM | whether a program removes a file is a fact about the program; both ends are wired | 'die Hälfte der Zerstörungsregel, die sagt WAS zerstört, bleibt eine Vokabelliste (`guard_fs_tripwire.NAMING_DESTRUCTION` / `SWEEPING_DESTRUCTION`): ob ein Programm eine Datei entfernt, ist… |
| H131 | BUG-0214 | low | INSTR | the official form was not read and is not shipped | 'die Zeilennummern der Anlage EÜR, die das Kit ausliefert (`templates/project_memory/master_data.yaml`, `euer_form:`), stammen aus öffentlichen Ausfüllhilfen und NICHT aus dem amtlichen Vor… |
| H132 | BUG-0215 | medium | DESIGN | the plan approval breaks the one-item binding on purpose (FR-0074) | 'die Plan-Freigabe (`approvals.PLAN_KIND`) bricht absichtlich die Ein-Item-Bindung: EINE Antwort lässt jedes Ziel ihrer Liste die Kante `DRAFT -> APPROVED` gehen. Am Piloten außerhalb des R… |
| H133 | BUG-0216 | medium | DESIGN | the embedding program IS __main__ -- the third condition cannot hold on the SDK route | 'die zweite Mint-Route (`kernel/sdk_approval.py`, der Einstieg aus dem Agent-SDK-`canUseTool`) kann die dritte Bedingung von `approvals._assert_minting_caller` nicht tragen — das einbettend… |
| H134 | BUG-0217 | medium | DESIGN | a value cannot measure its own truth | '`blocked` ist ein Zustand und keine Messung: niemand prüft nach, ob der Browser wirklich fehlte. Gemessen am Piloten mit dem ausgelieferten `gate_git.py` als Prozess: ein `blocked` ohne Sa… |
| H135 | BUG-0218 | low | INSTR | the witness half is a sample; a glob language is a build | 'die Zeugen-Hälfte der Überlappungsprüfung ist eine STICHPROBE, keine Sprache: `witnesses` füllt jeden Wildcard-Lauf mit EINEM Platzhalter-Segment, also findet sie eine Überlappung nur dort… |
| H139 | BUG-0222 | medium | DESIGN | building the BUILD half of FR-0077 is a feature; PR-0012 builds no FR | 'die BUILD-Hälfte von `FR-0077` (C1/C2/C3 im `browser_smoke()`, B3 über `kit_checks._frontend_sources()`) ist nicht gebaut: beide Wirtsdateien liefern dev-team und research-team **byte-glei… |
| H140 | BUG-0223 | low | DESIGN | the rule judges what the draft DECLARES; contrast over an image is not decidable | 'die Rangfolge-Regel urteilt über `[data-view]`-Container, also über das, was der Entwurf DEKLARIERT: dasselbe Markup mit zwei `data-primary-action` ist **rc 3** mit `data-view` und **rc 0*… |
| H141 | BUG-0224 | low | ENUM | 'is this sentence a cadence' is world knowledge, not a derivation from the tree | '`tools/test_parallel_streams.py`s `_CADENCE_IN_PROSE` ist eine AUFZÄHLUNG von Takt-Adverbien (`weekly`, `daily`, `monthly`, `every week` …). Ein Takt, der anders geschrieben ist, ist ihr u… |
| H142 | BUG-0225 | low | DESIGN | stricter breaks the argument-free run outside a project | 'die Überlappungsprüfung unterscheidet seit dieser Nacharbeit, was der Aufrufer GENANNT hat (ein `--root`, das kein Verzeichnis ist, und eine `--only`-Id, die kein Auftrag trägt → rc 1) von… |
| H143 | BUG-0226 | low | DESIGN | the seam is the orchestrator's DECLARATION and is printed line by line | 'eine erklärte Naht, die die GESAMTE Ownership eines der beiden Aufträge deckt, wird seit dieser Nacharbeit verweigert (rc 1) — eine, die nur einen TEIL der Überlappung deckt, während beide… |
| H147 | BUG-0230 | medium | PROV | date.fromisoformat's accepted spellings are the interpreter's | 'welche Datums-Schreibweisen ein `MST` annimmt, entscheidet der Interpreter: `backlog_types.normalised_date` ist `date.fromisoformat`, und die liest `20261001` erst ab 3.11 — dieselbe Erfas… |
| H148 | BUG-0231 | medium | DESIGN | the obvious rule would forbid the legitimate glob seam team-kits/*/VERSION | 'eine deklarierte Naht darf breiter sein als das, was zwei Aufträge wirklich teilen. `kernel.scopes.owns_anything_outside` verweigert nur den Totalfall — eine Naht, die einem der beiden Auf… |
| H151 | BUG-0233 | medium | OVERREF | .claude/hooks/_harness.py derives the area from MODULES -- forbidden_scope | 'die Erklärung, auf der Gate 5 entscheidet (`tools/test_surface.json`), liegt ausserhalb seines eigenen Schutzbereichs: `_harness.decision_inputs` leitet ihn aus den geladenen MODULEN ab, u… |
| H152 | BUG-0234 | medium | ENUM | what a value HITS only a collection knows, and a collection is the cost the gate exists against | 'die Aufzählung `options_that_narrow` sagt, nach welcher Option eine Zeile die erklärte Fläche nicht mehr ganz fährt — eine Tatsache über den LÄUFER, die keine Eigenschaft des Textes entsch… |
| H153 | BUG-0235 | medium | INSTR | a runner name the shell creates is not in the text | '(a) einen Läufernamen, den erst die Shell herstellt, sieht der billige Vorfilter beider Gate-Hälften nicht — gemessen: `pytest tools/` ist **rc 2**, `R=pytest; $R tools/` ist **rc 0**. (b)… |
| H155 | BUG-0237 | medium | DESIGN | a closed vocabulary for `class` is a contract change with a migration | 'das Feld `class` eines Wurzelziels hat in diesem Kernel KEINE Vokabel: kein Schema, kein Validator und keine Erfassung schraenkt den Wert ein, und die ausgelieferte Suite allein erfasst `f… |
| H156 | BUG-0238 | medium | DESIGN | check-scopes covers the open orders; the lease sees running ones | 'die Dispatch-Verweigerung bei ueberlappenden Bereichen vergleicht den neuen Auftrag nur gegen Leases, die NOCH LAUFEN. Zwei READY-Auftraege mit demselben `allowed_scope`, die nie gleichzei… |
| H157 | BUG-0239 | medium | DESIGN | a gate over order prose falls wrong in both directions (FR-0010) | 'kein gebauter Leser urteilt über den WORTLAUT einer Auftragszeile: ein `TSK`, dessen `expected_outputs` einen Baustein nennt, den kein Abnahmekriterium seines Ziels nennt, wird erfasst und… |
| H158 | BUG-0240 | medium | DESIGN | an occasion-driven retrospective is a build, not a fix | 'die Rückschau hängt an Anlässen, gebaut ist ein Takt: `_routine.routine_duties` antwortet allein aus Datum und Laufprotokoll. Gemessen je Kit über zwei Projekten desselben Tages — eines le… |
| H159 | BUG-0241 | medium | INSTR | a reader without the backtick decoration goes red on prose | 'der erste Leser der drei Harness-Rollentexte löst Item-Ids und Test-Knoten auf und sieht drei Sorten Zeiger nicht: einen Testnamen ohne Backticks, eine Behauptung, die gar keinen Test nenn… |
| H160 | BUG-0242 | medium | DESIGN | the content is the user's craft knowledge (DEC-0056 (c)) | 'ein Kit-Update, das `memory:` aus einer Rolle entfernt, laesst den Gedaechtnisbaum liegen, den eine aeltere Version unter `.claude/agent-memory/<rolle>/` schrieb. `team-kits/kernel/kitupda… |
| H166 | BUG-0248 | medium | DESIGN | one ledger row carries one VAT rate; the refusal is correct and named in the contract | 'Until closed, the intake REFUSES rather than books: exit 2 with the rates named, nothing staged, nothing booked (tools/test_office_package.py measures the single-rate path; the refusal bra… |
| H169 | BUG-0251 | medium | PROV | the Agent tool has no per-spawn effort parameter | the RUNG axis is applied (the Agent tool's `model` parameter overrides the child's pin, measured 2026-09-05 21:43, rig `probe-agent-override`; `gate_dispatch` refuses a spawn below the leas… |
| H170 | BUG-0252 | low | DESIGN | a PROC root carries no `class` field | the default (`medium`) is the value DEC-0078 named for ordinary office work, so nothing runs too high; the office declaration and constitution state the gap in their own words |
| H171 | BUG-0253 | low | DESIGN | DEC-0078 (4) forbids a default ladder without a kit declaration | the harness roles' pins are the user's decision for generation 5 (DEC-0077 (4), DEC-0080 (8)) and stand in the role files; the lease says `absent` rather than pretending a rung, so a reader… |
| H172 | BUG-0254 | low | INSTR | no Codex CLI on the measuring host; the vendor pages contradict each other | the shared vocabulary the generator accepts (`low/medium/high/xhigh/max`, `gen_provider_artifacts.providers_from_project_config`) is one every vendor page lists, so no kit source can carry … |
| H173 | BUG-0255 | low | PROV | the Agent tool is not hookable on Codex (openai/codex#32753, closed as not planned) | Codex support is BETA (README); the Claude side holds the rung at the spawn; on Codex the lease and header still say what the state derived, so the answer is readable even where it is not e… |
| H175 | BUG-0257 | low | INSTR | no property of a backtick span separates an illustration from a pointer | 'What takes the place of it: the sweep is a REPORT, not a gate -- it exits 1 and no hook waits on it, so a noisy finding costs a reader a second and never blocks a merge. `kernel.report.poi… |
| H177 | BUG-0259 | low | DESIGN | three defensible defaults, a human in the loop by construction | 'What limits it today: the three values are the least loaded terms of the document and each is a defensible German-business default, none of them is a figure that is booked or a deadline an… |
| H178 | BUG-0260 | low | DESIGN | the climb counts FAILED RUNS; the miss direction is a more expensive model | 'The climb is bounded and cheap in the wrong direction: it goes UP one rung per failed run and is capped at the role''s top (`ladder_for_order`, measured on all three pilots), so the failur… |
| H179 | BUG-0261 | medium | DESIGN | a run outcome is not a verdict -- the user's click is (DEC-0100) | 'What takes the place of it: nothing invents a verdict. Every row of the table carries its measured line and its verdict WORD is the measurement, not a status write -- no item was transitio… |
| H182 | BUG-0264 | medium | DESIGN | .claude/hooks/gate_spawn_needs_item.py + _harness.py -- forbidden_scope, PATCH for the user | 'The four sentences GRANT nothing and refuse nothing: `gate_spawn_needs_item` decides the exemption on the frontmatter key `harness_item:` in the role''s own definition, never on the word '… |
| H183 | BUG-0265 | low | ENUM | INSTALLER_SCRIPT_DIRS carries a tripwire at both ends already | 'What takes the place of it: the enumeration carries a tripwire at BOTH ends in `tools/test_pointer_sweep.py::test_the_root_files_the_sweep_skips_are_installed_and_would_be_noisy` -- every … |
| H186 | BUG-0269 | low | PROV | the Desktop app holds day, hour and enabled flag; nothing here can query it | 'The damage is bounded and named. What the repository cannot read -- the task''s day, hour and enabled flag -- it does not claim: `starts_itself` needs the lead''s record AND the report cad… |
| H187 | BUG-0270 | low | INSTR | widening to tools/ needs H175 first, or it buys a false red | 'Not closed in this round and why: widening the subject to `tools/` turns the four fixture strings of `tools/test_pointer_sweep.py` red at once, and telling an ILLUSTRATION from a pointer i… |
| H188 | BUG-0272 | low | OVERREF | .claude/hooks/gate_lead_write_scope.py -- forbidden_scope | 'An over-refusal, no hole in the protection: nothing gets through, a read-only line is turned away. What it costs is measured on this round -- two refused lines, one of which silently dropp… |
| H189 | BUG-0273 | low | ENUM | a cadence alone is read as intent; the word list is the claim reader | 'The damage is bounded: the reader''s subject is three short texts (radar/README.md, two watcher definitions), all rewritten in generation 5 and read by the merge verifier sentence by sente… |
| H190 | BUG-0274 | low | ENUM | RE-CUT 2026-09-12 after reading the mechanism: catching the README's own wordings for the rejected option needs world knowledge, the H141 class -- a … | 'No protection is lost today: the shipped README makes no cloud claim, and the three listed names catch the phrasings anyone has actually typed (m10, v01, v02). What is open is the class --… |
| H196 | BUG-0280 | medium | INSTR | 55 of 65 call sites; the ambient tree is a throwaway and the failure direction is loud | 'What takes the place of the check, measured rather than hoped: (1) the ambient value is a THROWAWAY tree (`.pytest_cache/ambient-project/`), so the worst a forgetful call site does is meas… |

