# TSK-0140 protocol -- written as the work happens (DEC-0095 (6): a successor reads THIS, not a transcript)

Base: main checkout, HEAD `1b6d95c`. One writer. Scratch: `C:/Offline Repos/v2-testbed/_round-scratch/TSK-0140/`.

## Vorgefunden (06:21)

* 128 BUG items carry `hole_number` in `bugs/active` (the item said ~126).
* MEASURED FIRST, before any triage judgement (`_round-scratch/TSK-0140/naming_scan.py`, reader
  `kernel.naming_tests.nodes_naming`, patterns `tools/test_*.py`, `.claude/hooks/test_*.py`,
  `team-kits/*/hooks/test_*.py`): **1 of 128** holes is named by a test in DEC-0100 (3)'s declared
  place -- H184/BUG-0266. So "ALREADY-CLOSED" is almost empty by construction and every CLOSE in
  this order means a NEW test that names the bug.
* `docs/POST_V2_WISHLIST.md` carries 0 `### H` entries and 44 `### L` entries: the H half is the
  generated pointer index at the end of the document (`tools/migrate_holes.py --reindex`).
* The kernel already has the approval kind `hole_exception` with the edge
  `("BUG", HOLE_EXCEPTION_KIND) -> ("TRIAGED", ACCEPTED_EXCEPTION)`, ITEM-bound
  (`approvals.item_subject_manifest`). It has NO batch form: `cli.kinds_reading_argument("batch")`
  derives the batch kinds through `LINE_MANIFEST_RESOLVERS`, and only `verification` reads it.

## The plan, and the way it REJECTED (FR-0084)

Build the exception route as a BATCH form of the existing `hole_exception` kind, mirroring
`verification --batch`. REJECTED: asking the 104 exception rows as 104 single-item
`request-approval hole_exception BUG-nnnn` questions -- the shipped route can carry them today and
needs no code. It loses because PR-0012's own cost rule bounds the user at ~10 clicks and the
measured history of the smaller way is in DEC-0100's context: the per-item route for ~100 repaired
bugs was never clicked once, and the stock lied upward by that number for five days. What the
smaller way would NOT have covered: nothing technically, and everything practically.

## HALF report (06:29) -- the triage table

`project_memory/staging/TSK-0140/triage-table.md`, generated by
`_round-scratch/TSK-0140/make_triage.py` from the STORE joined with `triage_map.py`; the generator
refuses when store and map disagree in either direction, so a hole captured later cannot hide.

**128 holes: CLOSE 23, EXCEPTION 104, ALREADY-CLOSED 1.**

| class | CLOSE | EXCEPTION | ALREADY-CLOSED | total |
|---|---|---|---|---|
| OVERREF -- over-refusals of an enforcement gate | 2 | 10 | 0 | 12 |
| ENUM -- enumerations awaiting a definition | 2 | 6 | 0 | 8 |
| PROV -- provider and platform limits | 0 | 12 | 0 | 12 |
| INSTR -- limits of the measuring instrument | 11 | 23 | 0 | 34 |
| DESIGN -- bounds taken by design | 8 | 53 | 1 | 62 |

## Work log

### 1. The two pre-existing reds of the "no model on the spawn" class -- CLOSED (06:31-06:34)

MEASURED first, the arbiter's own line:
`tools/test_hooks_v2.py::test_the_lead_of_a_scaffolded_project_is_not_read_as_its_own_subagent`
failed at 1b6d95c with

> the lease for TSK-0001 says rung opus but the spawn names no model, so the child would run on the
> role's own pin (sonnet) -- the climb the state derived would not happen (DEC-0077 (2), DEC-0034).

MECHANISM: both fixtures spawn in a project with a KIT INSTALLED, so the lease carries a derived
rung; since generation 5 `dispatch.spawn_model_refusal` refuses a climbed spawn that names no
model. The fixtures were written before the ladder existed and measured that refusal instead of
their own subject.

FIX: `tools/test_hooks.py::model_the_lease_requires` asks the shipped predicate itself -- "would you
refuse a spawn with no model?" -- and hands back the rung exactly then; both fixtures read it off
the lease. Not a second statement of DEC-0077 (2).

* red: the two nodes at 1b6d95c (lines above), green after: `2 passed in 10.75s` and
  `tools/test_research_chain.py::…_through_the_shipped_hooks` `1 passed in 36.88s`.
* the helper's own both-ways test: `tools/test_hooks.py::test_the_lease_model_helper_answers_both_ways`.

### 2. The pinned-sections sweep -- said, then re-pinned ONCE (06:34)

17 sections CHANGED, **0 GONE, 0 NEW, 0 renamed**. Read against `git diff 6a412d8..1b6d95c` (6a412d8
is the last commit that wrote `tools/constitution_section_pins.json`) before re-pinning. WHAT
DRIFTED, three families, all of it committed generation-6 work:

* **(a) the two PM definitions' preamble** (dev + research `agents/project-manager.md`): frontmatter
  `model: fable` -> `model: lead` (DEC-0095 (2): the lead seat moved off the top rung).
* **(b) the enforcement and work-loop sections of all three constitutions** (dev §2/§5a, research
  §2/§5a, office §4a/§8): the new clause "THE USER'S HAND IS NEVER THE ROUTE AROUND A GATE"
  (BUG-0080/BUG-0081) and the reading-discipline duty (DEC-0095 (6)).
* **(c) every models/presets section and both PM SKILL work loops** (dev §11, research §11, office
  §7, the three SKILLs): the ladder rewrite -- DEC-0095 (1)/(4) (the build class starts on opus,
  the top rung is bought not chosen), DEC-0096 (the effort climbs before the rung), DEC-0097 (1)/(2)
  (a test-shaped acceptance may be asked down to sonnet) -- plus the wireframe-is-NOT-in-the-scope-
  manifest correction (BUG-0077) and the batch-verification paragraph (DEC-0100).

Re-pinned once with that note: `python tools/pin_constitution_sections.py --write --note "..."`,
`recorded 17 change(s)`; the node is green (`1 passed in 1.08s`).

### 3. BUG-0067's naming test under a WSL bash -- CLOSED at its mechanism (06:36-06:47)

MEASURED (`_round-scratch/TSK-0140/wsl_home_probe.py`, then `home_probe2.py`), two facts, and the
second one corrected my first fix:

* `C:/Windows/System32/bash.exe` (the WSL launcher, what the OS's own search resolves a bare `bash`
  to on this host) does NOT carry `HOME` across: sent `/mnt/c/...`, got `/home/giazenx`. WSLENV
  governs what crosses and HOME is not in it.
* Git Bash DOES carry the directory but REWRITES the spelling: with `HOME=C:/Users/.../home` it
  reports `$HOME` as `/tmp/.../home`. So an equality probe on the string calls a healthy host sick
  -- my first version did exactly that and skipped the measurement on the one host that produced
  BUG-0067. The property is REACHABILITY, not spelling.

FIX: `tools/test_hooks.py::_this_bash_finds_under_home` asks that bash whether the template tree is
reachable through the HOME this process sets; the seeding test skips with the BUG-0067 reason when
it is not. A check for the word "WSL" would be an enumeration of launchers.

RED-FIRST, in the `.git`-less rig copy (`rig.py mutate bug_0067`, the skip route removed), arbiter
`python -B -m pytest tools/test_hooks.py::test_the_seeding_script_refuses_the_tree_that_ships_the_templates`
with `PATH=/c/Windows/System32:$PATH`:

* without the route: **1 failed** --
  `AssertionError: Templates not found: /home/giazenx/.claude/team-kits/dev-team/templates/project_memory`
* with the route, same PATH: **2 skipped** (the route fires, with its reason)
* with the ordinary PATH: **2 passed** -- the measurement still RUNS on Git Bash, which is what the
  docstring demands ("a skip here would leave the measured case unmeasured on the only machine that
  has ever produced it").

Both directions of the probe itself:
`tools/test_hooks.py::test_the_bash_home_probe_tells_a_launcher_that_carries_home_from_one_that_drops_it`.

### 4. The batch route for ACCEPTED_EXCEPTION -- BUILT (06:40-07:05)

MEASURED FIRST, against the shipped kernel: the approval kind `hole_exception` and the edge
`("BUG", hole_exception) -> ("TRIAGED", ACCEPTED_EXCEPTION)` already existed, ITEM-bound; the batch
form did not -- `cli.kinds_reading_argument("batch")` derives the batch kinds through
`LINE_MANIFEST_RESOLVERS` and only `verification` read it. So AC-4's 105 exception rows would have
been 105 questions. Built as `verification --batch`'s twin, in `team-kits/kernel/approvals.py`:

* `hole_exception_batch(state, ids)` -- the records, refusing BY NAME what cannot be accepted;
* `hole_exception_subject_manifest(holes)` -- empty and >`BATCH_LIMIT` refused at the builder;
* `_hole_exception_target_form` / `_hole_exception_option_form` -- the sentence says how many gaps
  stay open, the APPROVING OPTION (the compared carrier) names each id WITH its bound, cut to
  `HOLE_BOUND_SHOWN` = 150 characters through the same `_one_line` fold `--reason` goes through;
* `batch_walk_end` -- an OFF-CHAIN target ends the walk there (the old derivation would have walked
  an ACCEPTED gap to VERIFIED, i.e. written "measured gone" for something nobody measured);
* `_close_what_the_batch_lists` -- an off-chain ending climbs the free edges to the edge's SOURCE
  first, because the automaton allows `ACCEPTED_EXCEPTION` only from TRIAGED and most of this
  repository's holes stand at OPEN;
* `content_question(kind)` -- a listed entry is bound to the content question ITS OWN kind asks,
  derived through `item_derived_kinds()`, so a hole is bound to `title/observed/expected/severity/
  limits/hole_number` and not to `_SCOPE_FIELDS` (which carries no `limits`, so the bound could
  have been rewritten under a standing acceptance);
* `cli._hole_exception_holes` + `LINE_MANIFEST_RESOLVERS["holes"]`, and the CLI's "no item" refusal
  now points at `--batch` instead of saying "drop the id", because this kind took a positional id
  until today.

THE DEFECT MY OWN FIRST CUT INTRODUCED, found before the verifier: it refused every item without
`hole_number`, which would have locked out BUG-0055 and BUG-0056 -- the two defects this order has
to put to the user. The shipped per-item form never asked for the number either. The property that
carries is the BOUND: an item states one when somebody judged it unclosable and wrote down what
limits it instead. TWO more of my own texts were measured wrong against a copy of the real store
and corrected: a remedy naming `update --set limits=...`, a flag the parser does not have (`update`
reads a JSON body on stdin), and a blanket "take them out of the batch" appended AFTER a remedy
that says to write the bound -- two contradicting instructions in one refusal.

RED-FIRST, rig `_round-scratch/TSK-0140/redfirst.py`, arbiter `tools/test_approvals_dispatch.py`:

| mutation | what it restores | arbiter |
|---|---|---|
| `walk_end_on_chain` | `batch_walk_end` without the off-chain branch | 4 failed (the whole route) |
| `no_bound_refusal` | the bound check gone | 1 failed -- `…_refused_from_the_exception_batch_by_name` |
| `no_option_form` | `OPTION_FORMS` entry gone | 1 failed -- `…_names_every_listed_hole_and_its_bound` |
| `content_question_is_always_scope` | every entry bound to the scope hash | 1 failed -- `…_covering_a_hole_whose_bound_moved` |

Baseline before each: rc 0.

AS A PROCESS, against a COPY of this repository's real store
(`_round-scratch/TSK-0140/store-copy`, 105 gaps in 13 batches, zero refused):
`PYTHONPATH=team-kits python -B -m kernel.cli --root . request-approval hole_exception --batch
BUG-0102 BUG-0105` prints the question, the approving option carrying both bounds. The two defects
run end to end: `update BUG-0055 < …/bug-0055-limits.json`, same for BUG-0056, then one question
over both -- measured, and refused by name while either bound was still missing.

### 5. The four closed holes with new code, and two closed by naming what already measured them

| hole | bug | what was done | red-first |
|---|---|---|---|
| H185 | BUG-0268 | `CLAUDE.md` said "die vier Gates" with five registered and four table rows. The COUNT is out of the prose entirely now (a number lives in one place); the table is the one statement and is measured against `.claude/settings.json` | `claude_md_hides_gate_five` -> `.claude/hooks/test_gates.py::test_the_gate_table_of_this_file_is_the_registration_itself` red |
| H192 | BUG-0276 | nothing measured a `tools/` module shadowing a `team-kits/` one | `shadowing_module_under_tools` -> `tools/test_repo_hygiene.py::test_no_module_under_tools_shadows_one_of_the_kit_tree` red |
| H137 | BUG-0220 | the mirrored `hooks/_routine.py` module docstring claimed a cadence for three constitutions that state none; the claim is gone and the module docstring now has a reader | `routine_docstring_claims_the_constitutions_cadence` -> `tools/test_parallel_streams.py::test_the_shipped_routine_module_claims_no_cadence_for_a_text_it_does_not_own` red |
| H176 | BUG-0258 | the store-reading replacement existed and did not NAME the hole (DEC-0100) | `hole_index_reader_looks_for_the_old_headings` -> `tools/test_repo_hygiene.py::test_every_hole_is_one_index_row_one_prose_file_and_one_item` red |
| H167 | BUG-0249 | the brief already carries rung and effort; the test did not NAME the hole | `brief_drops_the_ladder_fields` -> `tools/test_report.py::test_the_session_brief_shows_the_rung_and_effort_a_lease_wrote_on_the_task` red |
| H195 | BUG-0279 | `kernel/naming_tests.py` already reads the DECLARED place; the test did not NAME the hole | `naming_accepts_any_mention` -> `tools/test_close_measured_pass.py::test_the_evidence_comes_from_the_tests_that_name_the_bug_and_from_no_other_node` red |

All six resolve through `kernel.naming_tests.nodes_naming` after the change (measured).

### 6. Re-cut and found

* **H190 / BUG-0274 re-cut CLOSE -> EXCEPTION**, after reading the mechanism rather than the title:
  catching the README's OWN wordings for the rejected cloud option ("hosted code routine of the
  platform") needs world knowledge -- the H141 class -- and a both-ends tripwire on
  `cloud_option.named_as` would not close what the item reports.
* **FOUND, pre-existing, fixed where it stood**: `tools/test_approvals_dispatch.py::
  test_a_batch_mint_leaves_the_index_as_fresh_as_a_per_step_rebuild_would` compared the whole
  `index.yaml` including `generated_at`, which has SECOND resolution -- it passed solo (52.1 s) and
  failed inside a six-suite run of 442 s on the same tree. The stamp is out of the comparison and
  its existence is asserted instead; every item row is still compared. Same class as BUG-0262/H180,
  another test.
* **THREE TRIPWIRES fired as designed** and were edited deliberately with the measurement they ask
  for: `test_presets.py::test_every_target_form_names_a_live_apr_kind` (the enumerated form set),
  `test_approvals_dispatch.py::test_the_batch_flag_belongs_to_the_kinds_whose_resolver_reads_it`,
  and `::test_only_a_kind_with_its_own_option_form_reads_differently_in_the_two_places` -- the last
  one no longer feeds one hard-coded manifest key but every key a list-bound builder declares
  (`cli.manifest_parameters`), so the third batch kind is measured by it on arrival.

### 7. AC-5 prepared, and the exact numbers (07:08-08:30)

* **ONE stamp, after the last kit change** (`python tools/bump_kit_version.py`, 07:08): dev/office/
  research all `2026.09.12-1`. Everything changed after it lies under `tools/` and is not kit
  material -- `python tools/validate.py` re-run at 08:29: `all structural checks passed`.
* **ruff** over `tools` and `team-kits`: `All checks passed!`
* **`report.stock_rollup`**: `Stock lies upward: 47` before this order's Evidence, **52** after it
  (six new passing `test` Evidences naming six bugs whose status still reads open, minus one
  already counted). It reaches **0** only when the lead mints the batches -- that is AC-2's and
  AC-4's click, not a builder's write. `0 error(s), 91 warning(s)`.
* **THE READING SUITES**, derived by grepping the callers of every predicate this order moved
  (`_round-scratch/TSK-0140/callers.py`: `listed_content_hash`, `batch_walk_end`,
  `content_question`, `LINE_MANIFEST_BUILDERS`, `kinds_reading_argument`, `OPTION_FORMS`,
  `TARGET_FORMS`, `hole_exception`, `_routine`, `model_the_lease_requires`,
  `_this_bash_finds_under_home`, `_registered_tools`, `_top_level_modules_of`, `nodes_naming`,
  `generate_session_brief`) -- 20 suites, all green:

  | run | result |
  |---|---|
  | test_routine_feed + test_parallel_streams + test_review_procedure + test_office_duties + test_repo_hygiene + test_close_measured_pass | 172 passed, 202 s |
  | `.claude/hooks/test_gates.py -k "registration or gate_table or claims_in_its_own_prose or hole"` | 15 passed, 166 s (the whole surface is gate 5's refusal for a stream -- correctly) |
  | test_approvals_dispatch (alone, after the tripwire edits) | 221 passed, 177 s |
  | test_light_kit + test_role_contracts + test_office_package + test_migrate | 276 passed, 557 s |
  | test_research_chain | 10 passed, 115 s |
  | test_hooks | 1040 passed, 13 skipped, 1504 s |
  | test_hooks_v2 | 2157 passed, 1281 s |

  The first pass of the group `test_approvals_dispatch + test_kernel + test_presets +
  test_staging_cli + test_report + test_board` (442 s) had **4 failed**: three tripwires firing as
  designed (section 6) and the `generated_at` clock sensitivity (section 6). All four resolved.
* **NO FULL RUN** -- AC-5's is the goal round's, after the last rework.
* **`migrate-holes --reindex` is DUE** once the lead has minted anything: the six closed holes move
  to VERIFIED and the 105 accepted ones to ACCEPTED_EXCEPTION, and the index rows in
  `docs/POST_V2_WISHLIST.md` carry the status. It is the lead's line and it is not run here --
  nothing in the store has moved yet, so a reindex now would rewrite the document with the same
  content.

### 8. The (g) row for TSK-0140 (DEC-0088 cadence: one builder, one mid-goal check, one goal round)

Builder: opus / effort high, ONE agent, 06:21 -> 08:30 = **2 h 09 wall-clock**, no rework round yet
(the verifier has not run). Cost split, measured from the clock reads in this protocol:

* triage (the table over 128 items, including the naming scan): 06:21-06:29, **8 min**
* the three pre-existing reds + BUG-0067: 06:31-06:47, **16 min**
* the batch route for ACCEPTED_EXCEPTION incl. red-first: 06:40-07:05, **25 min**
* six hole closures incl. red-first: 06:50-07:08, **18 min**
* the exception lists (105 gaps, 13 batches, each built against a copy of the real store): **12 min**
* stamp + ruff + validate + **20 reading suites**: 07:08-08:30, **82 min**, of which 46 min is
  `test_hooks` + `test_hooks_v2` alone

The reading suites are 63 % of the wall-clock of this order. That is the number DEC-0088's cadence
question is about, and it is the one to carry into the goal round's planning.

### 9. What was deliberately NOT closed, and named

**17 holes the triage reads as CLOSE that this order did not reach.** They are neither closed nor
excepted, so they are the goal round's work and must not be minted as exceptions:

H47 (BUG-0139), H57 (BUG-0149), H66 (BUG-0158), H94 (BUG-0186), H111 (BUG-0195), H123 (BUG-0207),
H130 (BUG-0213), H154 (BUG-0236), H161 (BUG-0243), H164 (BUG-0246), H168 (BUG-0250), H180
(BUG-0262), H181 (BUG-0263), H191 (BUG-0275), H193 (BUG-0277), H194 (BUG-0278) -- and H190
(BUG-0274), which is RE-CUT to EXCEPTION and IS in the lists.

The two with a session-internal chain come first if the goal round has to choose: **H47** (`F=…;
> $F` writes canonical state past gate 1 -- the closing direction (a) is in THIS order's scope,
pulling the kit's line-assignment map into the shared target reader) and **H57** (an interpreter
heredoc is invisible to `gate_ledger_valid`, so the ledger's judge can be replaced in its body).

### 10. The patch the user applies from outside Claude Code

`project_memory/staging/TSK-0140/h182-harness-patch.md` -- H182 / BUG-0264, four schedule sentences
in `.claude/hooks/gate_spawn_needs_item.py` and `.claude/hooks/_harness.py`. Three text changes, no
behaviour; the item stays an EXCEPTION row until it is applied, because the class it names (a NEW
schedule claim in the enforcement layer) is not closed by the patch.

### 11. Evidence and the lines the lead runs

`EVD-0316` BUG-0268, `EVD-0317` BUG-0276, `EVD-0318` BUG-0258, `EVD-0319` BUG-0220,
`EVD-0320` BUG-0249, `EVD-0321` BUG-0279 -- each `kind test`, `result pass`, `run_scope selection`,
`run_command` the single node that NAMES the bug, artifact `staging/TSK-0140/run-bug-nnnn.log`.

Closing the six (MEASURED as a process, rc 0, against a copy of the real store with `tools/` and
`.claude/hooks/` beside it -- `_round-scratch/TSK-0140/verify_closing_lines.py`):

```
PYTHONPATH=team-kits python -B -m kernel.cli --root project_memory \
  request-approval verification --batch BUG-0220 BUG-0249 BUG-0258 BUG-0268 BUG-0276 BUG-0279
```

The 105 exception rows and the two defects: `project_memory/staging/TSK-0140/exception-batches.md`,
13 + 1 questions, every line built against a copy of the real store before it was written there.

---

## Nacharbeit nach der Prüfung (09:15-09:40) und der Kurswechsel des Nutzers (09:22)

**Der Nutzer hat die erste Ausnahme-Frage abgelehnt** — »warum ist das nicht behoben? ich werde
nicht blind zustimmen«. Er hat recht über das Verhältnis: 105 Zustimmungen gegen 22 Schließungen
ist nicht, was »alles beheben« hieß. Die Ausnahme-Listen bleiben als RECORD stehen und tragen jetzt
einen Sperrvermerk in der ersten Zeile; **Auftrag 3b versucht zuerst zu SCHLIESSEN**.

**F2 (blockierend) — die sechs geteilten `limits`.** BUG-0105/0106/0107/0110/0111/0115 (H13/H14/
H15/H18/H19/H23) trugen EINEN Satz, der über keines von ihnen etwas sagte. Jedes hat jetzt einen
eigenen, gegen den benannten Code gelesen (`_harness.decision_inputs` über `sys.modules`;
`Deadline`, die die registrierte Frist aus `settings.json` liest und VOR dem Kill verweigert; die
geliehenen Kit-Helfer, deren Bruch fail-closed ist). Geschrieben durch den Kernel
(`update`, JSON-Körper auf stdin), Bodies unter `staging/TSK-0140/bug-0nnn-limits.json`.

**F1** `cli.py`: die Prüfung auf eine positionelle Id steht jetzt VOR der auf die fehlende Liste —
sonst las ein Aufrufer mit der alten Gewohnheit »und keine wurde genannt«, während er eine genannt
hatte, und die Abhilfe warf sie weg. Das Verb ist neutral: `verification` schließt, was es listet,
`hole_exception` schließt gar nichts.
Rot ohne den Fix: `tools/test_approvals_dispatch.py::test_a_batch_kind_given_a_positional_id_is_sent_to_the_flag_with_that_id_in_hand`.

**F8** `limits` muss eine Zeichenkette sein — sonst faltet `_one_line` eine Liste zu ihrem `repr`
und der Nutzer unterschreibt `['a', 'b']` als das, was die Lücke begrenzt. Rot ohne den Fix:
`::test_a_bound_that_is_not_a_sentence_is_refused_before_it_becomes_text`.

**F11** die Freigabe-Karte zählt jetzt, was eine listengebundene Freigabe bindet (»für die 2
Einträge ihrer Liste« statt »für keinen Vorgang«); der Zähler kommt aus der HERKUNFT (der
verbrauchten Anfrage), nicht von einem Aufrufer. Rot ohne den Fix:
`::test_the_card_of_a_list_bound_approval_counts_what_it_binds`. Die drei `gate_approval.py`
bleiben byte-gleich.

**F9** gemessen und im `BATCH_LIMIT`-Kommentar festgehalten, ohne Änderung der Zahl: ein
`verification`-Bündel von SECHS kostete 34,7 s = ~5,8 s je Eintrag, zehn landen also bei ~58 s gegen
die 60-s-Sperrfrist. Die Zahl steht AN ihrer Grenze, nicht darunter; das `hole_exception`-Bündel ist
je Eintrag billiger (kein Evidenz-Gang).

**F3** die Ausnahme-Zeilen tragen jetzt die vier Spalten des Items: id | Mechanismus | warum hier
nicht geschlossen | Begrenzung. **F5** eine Korrekturzeile im Pin-Journal (die Notiz in den 17
Zeilen sagt 18 CHANGED; gemessen waren 17). **F6** `CLAUDE.md` sagt nicht mehr »keine Kit-Hooks« —
der Kit-eigene `gate_approval.py` steht zweimal in der Registrierung, mit dem Grund. **F7** die zwei
Präsens-Stellen in `test_gates.py` sind korrigiert; die zwei gesperrten Dateien stehen im Patch.
**F10** H190 ist als »auf die zweite Form vertagt« notiert, nicht als unschließbar.

**F4** BUG-0067 war beim Eintreffen der Nacharbeit bereits durch den Klick des Nutzers geschlossen
(die Sieben-Bündel-Freigabe um 09:21) — es hat auf dem schließenden Baum eine frische Evidenz
(`EVD-0322`, drei nennende Knoten, 3 passed). **`stock lies upward` steht jetzt bei 45**, und die 45
sind ALLE Loch-Items; davon sind GENAU SECHS unerreichte CLOSE-Löcher (BUG-0195, BUG-0207, BUG-0213,
BUG-0236, BUG-0243, BUG-0246). Nach allen Ausnahme-Klicks bliebe die Zahl also bei **6**, nicht bei
0 — AC-5's Null ist die des Zielrunde nach 3b.

## Die Tabelle für Auftrag 3b

`project_memory/staging/TSK-0140/order-3b-candidates.md`: **121 offene Löcher** — die 105
Ausnahme-Zeilen plus die 16 CLOSE-Löcher, die dieser Auftrag nicht mehr erreicht hat, jedes mit
id | Fehler | Klasse | Fix hier? | wo | ein Satz auf Deutsch.

**Die Spalte hat DREI Werte, nicht zwei**, weil ein Ja/Nein die Unterscheidung verdeckt, die sagt
WER handelt: **ja 95** (eine Datei, die eine Rolle hier schreiben darf), **gesperrt 8** (in diesem
Repo, aber in `.claude/hooks/**` — der Nutzer wendet es aus einer Shell außerhalb an; zwei Patches
liegen bereit), **nein 18** (Anbieter/Plattform oder Weltwissen — nur diese sind überhaupt
Kandidaten für eine abgenommene Ausnahme).
