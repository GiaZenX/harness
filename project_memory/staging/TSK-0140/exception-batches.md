# TSK-0140 exception lists -- every gap not closed, with the bound the user accepts

> **NOT TO BE ASKED AS THEY STAND (2026-09-12, 09:22).** The user refused the first of these questions: "warum ist das nicht behoben? ich werde nicht blind zustimmen" -- and he is right about the ratio: 105 acceptances against 22 closes is not what "alles beheben" meant. Order 3b attempts to CLOSE first; what it cannot close comes back here. The plan per gap, with the one question that decides who acts -- can this repository fix it at all -- is `order-3b-candidates.md` beside this file. These lists stay as the RECORD of what each gap costs and what bounds it today; the two batches OVERREF-1 and DESIGN-1 additionally carry six `limits` sentences rewritten on 2026-09-12, because the six had shared one sentence that said nothing about any of them.

Generated by `_round-scratch/TSK-0140/make_exception_batches.py` from the STORE and the triage map. Every batch below was BUILT against a copy of the real store through `approvals.hole_exception_batch` before it was written here, so a line that stands here is one the kernel accepts; a batch it refused is printed with the refusal instead of the line.

One question per batch, at most `approvals.BATCH_LIMIT` (10) gaps each. The user reads, per entry, the id and what bounds the gap TODAY; the approving option carries both and `gate_approval` compares it character for character.

## OVERREF (10)

*over-refusals of an enforcement gate -- nothing gets through, a harmless line is turned away*

### batch OVERREF-1 (10)

| hole | bug | sev | mechanism | why not closed here | what bounds it today |
|---|---|---|---|---|---|
| H13 | BUG-0105 | low | `_harness.decision_inputs` schützt genau die Dateien, aus denen die Antwort des Gates **berechnet** wurde (gemessen über `sys.modules`, nicht getippt). Ein *neues* File neben `tools/bump_kit_version.… | .claude/hooks/_harness.py (decision_inputs) -- forbidden_scope | what IS protected is every module the gate really loaded while deriving its answer -- `_harness.decision_inputs` asks the interpreter through `sys.mo… |
| H18 | BUG-0110 | low | ob eine Stufe lesend ist, beantwortet die Klassifikation der **Kits** (`gate_write_scope._stage_is_read_only`), und `cp`, `robocopy`, `xcopy` und `tar` sind dort nicht lesend — zu Recht, denn sie sch… | .claude/hooks/_harness.py -- forbidden_scope | an OVER-refusal, not a hole: nothing gets through, a read-only line is turned away. `cp`, `robocopy`, `xcopy` and `tar` are not read-only to the kits… |
| H19 | BUG-0111 | low | die Zugehörigkeitsprüfung antwortet in **beide** Richtungen, und sie muss das: `rm -rf team-kits` nennt jede geschützte Datei darunter, ohne eine davon zu buchstabieren. Die Kehrseite ist, dass ein K… | .claude/hooks/_harness.py -- forbidden_scope | the other half of the same over-refusal, and it is the price of a membership test that must answer in BOTH directions: `rm -rf team-kits` names every… |
| H23 | BUG-0115 | low | eine Dateisystemfrage zu einem nicht erreichbaren Host kostet auf diesem Host 42,1 s und lässt sich nicht abbrechen. Seit TSK-0011 ist die **Wartezeit** begrenzt: `_harness` liest die Frist aus der R… | .claude/hooks/_harness.py -- forbidden_scope | the DEADLINE half is closed and only the judgement is spent: `_harness.Deadline` reads the registered `timeout` out of `.claude/settings.json` and re… |
| H62 | BUG-0154 | medium | Seit TSK-0083 stellt `gate_ledger_valid` die Frage „schreibt diese Zeile?" je Pipeline-**Stufe** (`_stages_beside_the_vouched_runs`), die Köder-Frage aber weiter je **Segment** (`_DECOY_VALIDATOR_RX`… | the one-line candidate was measured and rejected (it reopens BUG-0064's neighbour) | die Köder-Prüfung des Ledger-Gates (`_DECOY_VALIDATOR_RX`) urteilt noch über das ganze Segment, während dieselbe Frage seit TSK-0083 sonst je Stufe g… |
| H64 | BUG-0156 | medium | Der F3-Fix der Runde (`_redirect_targets`, `finditer`) liest **alle** Umleitungsziele eines Segments, und zwar auf einer Sicht, die den Inhalt quotierter Spannen behält; die Redirect-Prüfung steht vo… | closing means knowing which `>` the shell executes | `gate_ledger_valid` liest seit dem F3-Fix JEDES `>` eines Segments als Umleitung — auch in quotierter Argument-Prosa, auch in den Argumenten eines ve… |
| H68 | BUG-0160 | medium | Das Ledger-Gate urteilt über **alle** Lesarten, die die Vorverarbeitung dem Text geben kann, und verweigert, sobald **irgendeine** davon „schreibt" sagt. Das ist die gebaute Richtung und die sichere.… | the obvious fix was measured wrong (it reopens BUG-0065) | zwei Schreibweisen, die das Ledger-Gate verweigert, obwohl sie nichts schreiben: ein **handgetippter Backslash** im Validatorpfad (`python scripts\le… |
| H69 | BUG-0161 | medium | `TSK-0084` hat zwei verschiedene Defekte derselben Zeichenfamilie geschlossen. Die Gates dieses Repos teilen sich mit den Kits die Vorbereitung (`_compat`), aber nicht die Tür, an der die zweite Hälf… | .claude/hooks/_harness.py does not go through _kernel.payload -- forbidden_scope | die Gates dieses Repos erben die CR-Härtung der Kits nur zur Hälfte: der Trenner-Teil sitzt in `_compat` und greift mit (rc 0 → rc 2), die **Verschwe… |
| H151 | BUG-0233 | medium | `gate_test_scope` (Gate 5 dieses Repos) entscheidet auf `tools/test_surface.json`: welche Flächen erklärt sind, ab welcher gemessenen Dauer sie überhaupt beurteilt werden, und welche Optionen die Zei… | .claude/hooks/_harness.py derives the area from MODULES -- forbidden_scope | die Erklärung, auf der Gate 5 entscheidet (`tools/test_surface.json`), liegt ausserhalb seines eigenen Schutzbereichs: `_harness.decision_inputs` lei… |
| H188 | BUG-0272 | low | MEASURED 2026-09-06 (TSK-0133, generation-5 merge), each line as a Bash tool call in the main checkout, so the hook itself judged it: * `cd "C:/Offline Repos/AgentAndSkills" && grep -c "\`a\`, \`b\`"… | .claude/hooks/gate_lead_write_scope.py -- forbidden_scope | An over-refusal, no hole in the protection: nothing gets through, a read-only line is turned away. What it costs is measured on this round -- two ref… |

```
PYTHONPATH=team-kits python -B -m kernel.cli --root project_memory \
  request-approval hole_exception --batch BUG-0105 BUG-0110 BUG-0111 BUG-0115 BUG-0154 BUG-0156 BUG-0160 BUG-0161 BUG-0233 BUG-0272
```

*built: 10 records*

## ENUM (7)

*enumerations standing in for a definition nobody has written yet*

### batch ENUM-1 (7)

| hole | bug | sev | mechanism | why not closed here | what bounds it today |
|---|---|---|---|---|---|
| H86 | BUG-0178 | low | **Anlass:** `FR-0044`/N13. Der Installer klassifizierte den vorgefundenen Bestand gar nicht; die Seitenkontrolle vom 2026-08-16 konnte den No-write-Nachweis nicht führen, weil ihr ein echter V1-Besta… | closing means a SECOND definition of 'V1 record' | drei Grenzen EINES V1-Erkenners, den `migrate` und `report` schon benutzen — ein Speicher außerhalb des Zustandsverzeichnisses, ein umbenannter darin… |
| H129 | BUG-0212 | low | Die WO-Hälfte der Zerstörungsregel ist eine Definition, die WAS-Hälfte nicht: `guard_fs_tripwire.NAMING_DESTRUCTION` und `SWEEPING_DESTRUCTION` sind Wortstämme. Ob ein Programm eine Datei entfernt, i… | whether a program removes a file is a fact about the program; both ends are wired | die Hälfte der Zerstörungsregel, die sagt WAS zerstört, bleibt eine Vokabelliste (`guard_fs_tripwire.NAMING_DESTRUCTION` / `SWEEPING_DESTRUCTION`): o… |
| H141 | BUG-0224 | low | `tools/test_parallel_streams.py::test_no_text_that_describes_the_audited_role_states_the_cadence_the_code_owns` hält die Regel „der Takt steht im Code, nicht ein zweites Mal im Rollentext“. Ob ein Sa… | 'is this sentence a cadence' is world knowledge, not a derivation from the tree | `tools/test_parallel_streams.py`s `_CADENCE_IN_PROSE` ist eine AUFZÄHLUNG von Takt-Adverbien (`weekly`, `daily`, `monthly`, `every week` …). Ein Takt… |
| H152 | BUG-0234 | medium | „Diese Zeile fährt die ganze erklärte Fläche" ist als Eigenschaft gebaut, mit genau einer unvermeidbaren Aufzählung: welche Optionen der Läufer so liest, dass die Zeile die Fläche NICHT mehr ganz fäh… | what a value HITS only a collection knows, and a collection is the cost the gate exists against | die Aufzählung `options_that_narrow` sagt, nach welcher Option eine Zeile die erklärte Fläche nicht mehr ganz fährt — eine Tatsache über den LÄUFER,… |
| H183 | BUG-0265 | low | MECHANISM: `kernel.report.installed_kit_paths` derives kit material from three readers the project holds (the shipped gate's `_ENFORCEMENT_PATHS`, the project's `.claude/provider_artifacts.json`, the… | INSTALLER_SCRIPT_DIRS carries a tripwire at both ends already | What takes the place of it: the enumeration carries a tripwire at BOTH ends in `tools/test_pointer_sweep.py::test_the_root_files_the_sweep_skips_are_… |
| H189 | BUG-0273 | low | MEASURED 2026-09-11 (TSK-0133, generation-5 merge, verify round 1 P6): `tools/test_radar_trigger.py` reads the three texts that say how a watcher run starts (`TEXTS`: radar/README.md and the two watc… | a cadence alone is read as intent; the word list is the claim reader | The damage is bounded: the reader's subject is three short texts (radar/README.md, two watcher definitions), all rewritten in generation 5 and read b… |
| H190 | BUG-0274 | low | MEASURED 2026-09-11 01:4x by the merge verifier (project_memory/staging/TSK-0133/verify-round-2.md, N1), one sentence inserted into radar/README.md per row, arbiter the whole tools/test_radar_trigger… | DEFERRED to the second form, not unclosable: the both-ends list check of the `test_radar_trigger.TEXTS` pattern (each listed name load-bearing, and the list complete for the texts the declaration itself publishes) is the shape that closes it; catching an ARBITRARY future wording stays the H141 class | No protection is lost today: the shipped README makes no cloud claim, and the three listed names catch the phrasings anyone has actually typed (m10,… |

```
PYTHONPATH=team-kits python -B -m kernel.cli --root project_memory \
  request-approval hole_exception --batch BUG-0178 BUG-0212 BUG-0224 BUG-0234 BUG-0265 BUG-0273 BUG-0274
```

*built: 7 records*

## PROV (12)

*provider and platform limits -- the client, the Agent tool, Codex, CPython, git*

### batch PROV-1 (10)

| hole | bug | sev | mechanism | why not closed here | what bounds it today |
|---|---|---|---|---|---|
| H36 | BUG-0128 | low | der Faden aus H35 kann nur unterbrechen, was der Interpreter zwischen zwei Bytecodes aus der Hand gibt. Ein einzelner Aufruf in eine C-Funktion tut das nicht. Der teuerste davon auf diesem Weg ist di… | CPython does not release the interpreter inside one C call | die Grenze ist gemessen und liegt außerhalb des Gates: die Zeilenlänge, ab der die nicht unterbrechbare Stelle die Frist reißt, kann auf diesem Host… |
| H49 | BUG-0141 | medium | `gate_subagent_output` blockiert einen Spezialisten, der ohne seinen Ausgabeblock stoppt. Der Provider setzt auf der dadurch ausgelösten Fortsetzung `stop_hook_active`, und der Hook ehrt dieses Flag:… | stop_hook_active: blocking twice is an endless loop | an die Stelle des Schutzes treten drei Begrenzungen: die erste Verletzung wird blockiert und präzise angeleitet; der Durchlass ist zustandsgenau prot… |
| H50 | BUG-0142 | medium | Der Untätigkeits-Melder (`kernel.dispatch.idle_dispatches`) urteilt nur aus positiven Datensätzen: dem vermerkten Kind-Ende (`SubagentStop`) oder einer vorliegenden Lease, die nie ein Kind gebunden h… | the provider never delivers the SubagentStop | Preis des F1-Fixes: ein gebundenes Kind, dessen `SubagentStop` nie ankommt, ist nach dem TTL-Sweep für den Untätigkeits-Melder unsichtbar. An die Ste… |
| H53 | BUG-0145 | medium | Der Melder hält still, wenn der Provider den Stop als bereits blockiert markiert. Ob dieser Schlüssel nur die unmittelbar ausgelöste Fortsetzung trägt oder länger steht, ist hier nicht gemessen (`pro… | the lifetime of the provider key is not measurable here | die Lebensdauer von `stop_hook_active` ist ungemessen; die Fehlrichtung ist stumm, nie schleifend, und kein ausgelieferter Text behauptet mehr als „a… |
| H78 | BUG-0170 | medium | **Der Mechanismus, in einem Satz** (so formuliert der Prüfer, und so gehört er gelesen — `--restricted` ist nur die Schreibweise, an der es auffiel): **jeder Client-Startmodus, der die Projekteinstel… | a client start mode that loads no project settings -- nothing buildable from inside | ein Client-Startmodus, der die Projekteinstellungen nicht lädt, entfernt alle vier Registrierungsflächen des Kits, während die Dateiwerkzeuge im Proj… |
| H89 | BUG-0181 | low | `gate_second_booking` urteilt nur über Ledger-Zeilen, die noch nicht in `HEAD` stehen. Das ist die Migrationsantwort: ein echtes Buch trägt Zeilen, die vor dieser Schicht gebucht wurden, und ein Mech… | without git the baseline cannot be drawn | ohne `git` kann die Vier-Augen-Buchung Alt- von Neuzeilen nicht unterscheiden und TRITT ZURÜCK statt zu verweigern; die Gegenrichtung säße auf genau… |
| H114 | BUG-0198 | medium | Seit Claude Code 2.1.246 tauscht `/cd` Einstellungen, Hooks, Skills und MCP-Server gegen die des Zielverzeichnisses aus. `${CLAUDE_PROJECT_DIR}` bleibt dabei — laut Doku absichtlich — auf dem **Start… | /cd swaps the registration, not ${CLAUDE_PROJECT_DIR} -- client behaviour | nach einem Verzeichniswechsel läuft die Registrierung des Ziels, aber die Hook-DATEIEN des Startorts (`${CLAUDE_PROJECT_DIR}` bleibt stehen, und gena… |
| H115 | BUG-0199 | medium | Der Changelog-Eintrag zu 2.1.246 führt „skills, and agents" unter dem, was nach einem `/cd` sofort greift. Gemessen greifen Hooks, Einstellungen, Skills und MCP-Server sofort — die **Rollenliste** un… | client behaviour: the role list does not travel | ein Verzeichniswechsel bringt Subagenten und `agent:`-Bindung des Ziels NICHT mit, obwohl der Changelog „agents" nennt — gemessen am Task-Fehler, an… |
| H147 | BUG-0230 | medium | `state.capture_preflight` prüft ein Datumsfeld mit `backlog_types.normalised_date`, und das ist `datetime.date.fromisoformat`. Welche Formen diese Funktion liest, hat sich über die Python-Versionen e… | date.fromisoformat's accepted spellings are the interpreter's | welche Datums-Schreibweisen ein `MST` annimmt, entscheidet der Interpreter: `backlog_types.normalised_date` ist `date.fromisoformat`, und die liest `… |
| H169 | BUG-0251 | medium | `kernel.dispatch.ladder_for_order` derives the effort per order (dev/research `high`, `xhigh` when the goal's `class` is large; office `medium` / `high`; the filing floor `low`) and `create_lease` wr… | the Agent tool has no per-spawn effort parameter | the RUNG axis is applied (the Agent tool's `model` parameter overrides the child's pin, measured 2026-09-05 21:43, rig `probe-agent-override`; `gate_… |

```
PYTHONPATH=team-kits python -B -m kernel.cli --root project_memory \
  request-approval hole_exception --batch BUG-0128 BUG-0141 BUG-0142 BUG-0145 BUG-0170 BUG-0181 BUG-0198 BUG-0199 BUG-0230 BUG-0251
```

*built: 10 records*

### batch PROV-2 (2)

| hole | bug | sev | mechanism | why not closed here | what bounds it today |
|---|---|---|---|---|---|
| H173 | BUG-0255 | low | `gate_dispatch.py` PreToolUse(Agent/Task) passes the spawn's `model` to `dispatch.validate_dispatch`, which refuses a spawn that names no model for a climbed order or the wrong one (`spawn_model_refu… | the Agent tool is not hookable on Codex (openai/codex#32753, closed as not planned) | Codex support is BETA (README); the Claude side holds the rung at the spawn; on Codex the lease and header still say what the state derived, so the a… |
| H186 | BUG-0269 | low | **Mechanism (DEC-0089, correcting DEC-0085 and the first version of this item).** A Claude Desktop LOCAL scheduled task for the radar half has existed on this host since 2026-06-30: `C:/Users/zenti/.… | the Desktop app holds day, hour and enabled flag; nothing here can query it | The damage is bounded and named. What the repository cannot read -- the task's day, hour and enabled flag -- it does not claim: `starts_itself` needs… |

```
PYTHONPATH=team-kits python -B -m kernel.cli --root project_memory \
  request-approval hole_exception --batch BUG-0255 BUG-0269
```

*built: 2 records*

## INSTR (23)

*limits of the measuring instrument -- a reader that cannot see a class, or a measurement this host cannot make*

### batch INSTR-1 (10)

| hole | bug | sev | mechanism | why not closed here | what bounds it today |
|---|---|---|---|---|---|
| H10 | BUG-0102 | low | eine Hälfte einer Fallunterscheidung, die kein Test unterscheidet, ist unbelegt — sie lässt sich löschen, ohne dass etwas rot wird. | an exhaustive mutation run over .claude/hooks/ is the unasked question, not a patch | die beiden gefundenen Hälften sind geschlossen und je durch einen roten Test gedeckt; für die dritte gibt es keinen Ersatz, sondern eine ungestellte… |
| H41 | BUG-0133 | low | Der Wächter (`test_every_check_this_apparatus_claims_in_its_own_prose_is_one_that_exists`, Leser `_points_into_this_file`) macht seit TSK-0009 einen in Backticks genannten Testnamen prüfbar: er muss… | four measured limits of the pointer guard, no chain | vier gemessene Grenzen des Zeiger-Wächters aus TSK-0009, je im Eintrag; keine berührt eine Gate-Entscheidung. Der lebende Bestand ist in den ersten d… |
| H45 | BUG-0137 | low | Anlass war `BUG-0051`: seit dieser Host ein WSL-Linux trägt, gewann der Windows-eigene `bash`-Starter die Shell-Auswahl der Suite („läuft `true`" genügte), obwohl er absolut benannte Pfade in ein fre… | two measured limits of the arbiter hardening | zwei gemessene Grenzen der Arbiter-Härtung aus TSK-0063, je im Eintrag: die Sitzungswache verlangt die Seh-Eigenschaft auch von shell-freien Registri… |
| H65 | BUG-0157 | medium | Jeder Pfadleser dieses Gates urteilt über den **Text**, den der Nutzer geschrieben hat. Stellt die Shell das Zielwort erst her — `$f`, `${…}`, `${X:-…}`, ein Glob —, dann steht das Wort, auf das die… | a PreToolUse hook has no shell state | ein Zielwort, dessen Pfad die Shell erst durch Expansion **herstellt oder beendet** (`$f`, `${…}`, `${X:-…}`, Glob), steht so nicht im Text — dieser… |
| H70 | BUG-0162 | low | **Mechanismus, als Klasse und nicht als Schreibweise:** `tools/test_hooks_v2.py::test_every_vouching_run_pattern_is_named_here` soll verhindern, dass eine vierte Ausnahme in `_stages_beside_the_vouch… | the completeness wire asks for PATTERNS, so a pattern-free exemption is invisible | der Vollständigkeits-Draht des Ledger-Gates fragt nach **Mustern** (zwei Leser, Vereinigung), also sieht er eine vierte Ausnahme nicht, die **gar kei… |
| H71 | BUG-0163 | low | **Anlass, und er ist der beste Grund für einen Eintrag:** `BUG-0068` kam aus dem **Live-Gebrauch** des Nutzers an seinem echten Büro-Projekt. Er aktualisierte es selbst, bekam vom Manager `cp`-Zeilen… | four measured limits of the merge-backlog reader | vier gemessene Grenzen des Lesers der Merge-Rückstandsliste, je im Eintrag: ein Eintrag, den `open()` mit etwas anderem als `OSError` ablehnt, bricht… |
| H73 | BUG-0165 | low | **Anlass:** `FR-0052` bringt die Regel „erst die Entscheidungen, dann der Code" in die Lead-Texte aller drei Kits — auf die Fläche, die beim Antworten wirklich geladen ist, mit dem ehrlichen Satz, da… | four measured limits of the decision-first guard | vier gemessene Grenzen der Entscheidungs-zuerst-Runde, je im Eintrag: jede Id in einem Begrenzer, der länger ist als sie selbst, bleibt ungeprüft (16… |
| H74 | BUG-0166 | low | **Anlass:** `FR-0011` wollte die Wandzeit je Runde runter, ohne eine Messung zu verfälschen. Geliefert: Prozessarten laufen gebündelt (−20…−27 % je Fenster, Spaltenauswahl Faktor 32–44), die Frist-Ph… | measured limits of the faster gate suite; host load is not a code fact | gemessene Grenzen der schnelleren Gate-Suite, je im Eintrag: kein Schutz gegen einen zweiten Läufer (und die Suite drückt als Nachbar selbst stärker,… |
| H77 | BUG-0169 | low | **Anlass:** `BUG-0073`, Live-Screenshot des Nutzers — eine Freigabe-Karte erreichte ihn als deutsch-englisches Gewebe, weil der Manager die Feldwerte englisch verfasste und keine Fläche sagte, welche… | no gate reaches free text | gemessene Grenzen der Wertsprache-Regel, je im Eintrag: kein Gate erzwingt sie (Freitext); der Prosa-Test liest Anker und Ehrlichkeit, nicht Richtung… |
| H79 | BUG-0171 | low | **Anlass:** `BUG-0075`, live im echten Office-Projekt des Nutzers am 2026-08-30, einen Tag nach `apply-proposal` (H76): der Produkt-Redakteur überarbeitete eine Inhaltsregel, legte sie als PROSA unte… | the derivation reads names, not intentions | gemessene Grenzen der Besitz-Ableitung für die Dokument-Schreibroute, je im Eintrag: eine erfundene Zweitdatei ohne `staging/`-Präfix sieht der Leser… |

```
PYTHONPATH=team-kits python -B -m kernel.cli --root project_memory \
  request-approval hole_exception --batch BUG-0102 BUG-0133 BUG-0137 BUG-0157 BUG-0162 BUG-0163 BUG-0165 BUG-0166 BUG-0169 BUG-0171
```

*built: 10 records*

### batch INSTR-2 (10)

| hole | bug | sev | mechanism | why not closed here | what bounds it today |
|---|---|---|---|---|---|
| H81 | BUG-0173 | low | `report.approval_mint_is_wired` beantwortet „kann eine Antwort des Nutzers hier etwas bewirken" und entscheidet damit zwei Sätze, die eine Rolle liest (der stderr-Hinweis an `request-approval` und de… | _invoked_scripts also carries doctor and the bundle trust check -- a round of its own with its own reds | der Mint-Leser irrt in beide Richtungen, beide gemessen: eine Registrierung mit quotiertem Pfad, der ein **Leerzeichen** enthält, zerlegt `_invoked_s… |
| H85 | BUG-0177 | low | **Anlass:** `FR-0068`/`FR-0070` (zwei fremde Skills unter Apache-2.0 übernommen) und `FR-0045` (das Bündelschema aus einem echten Claude-Design-Export). | no test of this suite goes to the network | die Herkunftsprüfung sieht keine UNMARKIERTE Textänderung (kein Test dieser Suite geht ins Netz; ersetzt durch `source_commit`/`source_blob_sha1` im… |
| H109 | BUG-0193 | medium | `INV.verified` hat seit dieser Runde einen Produzenten (`state.record_invariant_verification`) und einen Merge-Blocker (`report._check_invariant_checks`, ein Validator-Error, auf dem `gate_memory_com… | the kernel parses, it does not run | „auflösbar" heißt für den Kernel: die Datei ist da und definiert den Namen — GEPARST, nicht gefahren. Ein Test, der existiert und übersprungen wird (… |
| H112 | BUG-0196 | low | FR-0038 verlangt „einen aufgezeichneten Zeitpunkt des letzten Audit-Laufs und eine Wochen-Id gegen Doppelläufe". Dieser Strom durfte keinen Hook registrieren (`settings/settings.json` liegt außerhalb… | the run record is a by-product of the event log | der Laufdatensatz der Routine ist ein Nebenprodukt des Ereignis-Logs und sagt „ein Subagent dieser Rolle hat aufgehört", nicht „ein Audit ist gelaufe… |
| H122 | BUG-0206 | low | `test_repo_hygiene.test_docs_prose_nothing_reads_any_more_is_reported_not_failed` nennt eine Datei unter `docs/`, die seit `_DOCS_GRACE_DAYS` niemand mehr anfasst und die NICHTS nennt. Sein Korpus is… | the corpus is what git carries; the blind field is empty today | der Melder über ungelesene Prosa unter `docs/` fragt, was **git trägt** (`test_repo_hygiene._carried_files`, `git ls-files -c -o --exclude-standard`)… |
| H131 | BUG-0214 | low | Das Kit liefert seit dieser Runde ein Standardvokabular aus, in dem jede Kategorie die Zeilennummer der Anlage EÜR trägt (`team-kits/office-team/templates/project_memory/master_data.yaml`, Block `eue… | the official form was not read and is not shipped | die Zeilennummern der Anlage EÜR, die das Kit ausliefert (`templates/project_memory/master_data.yaml`, `euer_form:`), stammen aus öffentlichen Ausfül… |
| H135 | BUG-0218 | low | Die Überlappungsprüfung (seit TSK-0120 nur noch `kernel.scopes`) fragt „gehört dieser Pfad beiden Aufträgen?“ mit EINEM Prädikat (`gate_write_scope._matches`) über ZWEI Universen: die Dateien, die de… | the witness half is a sample; a glob language is a build | die Zeugen-Hälfte der Überlappungsprüfung ist eine STICHPROBE, keine Sprache: `witnesses` füllt jeden Wildcard-Lauf mit EINEM Platzhalter-Segment, al… |
| H153 | BUG-0235 | medium | **Mechanismus (a) — der Vorfilter.** Beide Hälften des Gates suchen zuerst in der rohen Zeile (ohne Quotierungszeichen) nach dem Namen eines erklärten Läufers. Das ist der Preis dafür, dass das Gate… | a runner name the shell creates is not in the text | (a) einen Läufernamen, den erst die Shell herstellt, sieht der billige Vorfilter beider Gate-Hälften nicht — gemessen: `pytest tools/` ist **rc 2**,… |
| H159 | BUG-0241 | medium | `.claude/agents/` wird von keiner anderen Suite gelesen: `tools/test_repo_hygiene.py::_texts_that_answer_for_a_claim` läuft über `team-kits/` und `docs/`, `.claude/hooks/test_gates.py` über sein eige… | a reader without the backtick decoration goes red on prose | der erste Leser der drei Harness-Rollentexte löst Item-Ids und Test-Knoten auf und sieht drei Sorten Zeiger nicht: einen Testnamen ohne Backticks, ei… |
| H172 | BUG-0254 | low | DEC-0076 (3) asks that the Codex effort ceiling be measured against the CLI before any note claims it. radar/2026-09-05-codex.md item 3 measured the contradiction (model pages: `max`; the CLI agent-c… | no Codex CLI on the measuring host; the vendor pages contradict each other | the shared vocabulary the generator accepts (`low/medium/high/xhigh/max`, `gen_provider_artifacts.providers_from_project_config`) is one every vendor… |

```
PYTHONPATH=team-kits python -B -m kernel.cli --root project_memory \
  request-approval hole_exception --batch BUG-0173 BUG-0177 BUG-0193 BUG-0196 BUG-0206 BUG-0214 BUG-0218 BUG-0235 BUG-0241 BUG-0254
```

*built: 10 records*

### batch INSTR-3 (3)

| hole | bug | sev | mechanism | why not closed here | what bounds it today |
|---|---|---|---|---|---|
| H175 | BUG-0257 | low | MECHANISM: `kernel.report.pointer_sweep` reads a backtick span and asks two questions -- does this test node resolve in the tree, does this item id resolve in THIS store. A span that teaches by examp… | no property of a backtick span separates an illustration from a pointer | What takes the place of it: the sweep is a REPORT, not a gate -- it exits 1 and no hook waits on it, so a noisy finding costs a reader a second and n… |
| H187 | BUG-0270 | low | MEASURED 2026-09-06 (TSK-0131, closing round, self-found). `tools/test_repo_hygiene.py::_texts_that_answer_for_a_claim` walks two trees -- measured with its own reader: top-level directories read = [… | widening to tools/ needs H175 first, or it buys a false red | Not closed in this round and why: widening the subject to `tools/` turns the four fixture strings of `tools/test_pointer_sweep.py` red at once, and t… |
| H196 | BUG-0280 | medium | TSK-0139 measured the class twice in one round. BUG-0052's fix points the suite's ambient CLAUDE_PROJECT_DIR at a throwaway tree so no hook the suite starts can append to this repo's canonical audit… | 55 of 65 call sites; the ambient tree is a throwaway and the failure direction is loud | What takes the place of the check, measured rather than hoped: (1) the ambient value is a THROWAWAY tree (`.pytest_cache/ambient-project/`), so the w… |

```
PYTHONPATH=team-kits python -B -m kernel.cli --root project_memory \
  request-approval hole_exception --batch BUG-0257 BUG-0270 BUG-0280
```

*built: 3 records*

## DESIGN (53)

*bounds taken on purpose -- a contract decision, a fail-soft trade, prose where no gate reaches*

### batch DESIGN-1 (10)

| hole | bug | sev | mechanism | why not closed here | what bounds it today |
|---|---|---|---|---|---|
| H14 | BUG-0106 | low | die Abhilfe des Gates ist `kernel.cli evidence --result pass --summary <digest>`, und genau die *darf* der Sitzungsagent ausführen — er muss es, denn er zeichnet das Urteil des Prüfers auf. | the remedy gate 3 prints must be runnable by the lead | the remedy gate 3 prints (`kernel.cli evidence --result pass --summary <digest>`) is one the session agent MUST be able to run -- it is how the verif… |
| H15 | BUG-0107 | low | die Shell-Hälfte von Gate 1 und die Zeilenform-Prüfung von Gate 3 benutzen `team-kits/<kit>/hooks/gate_write_scope` als **Modul** — Tokenizer, Pipeline-Trennung, Read-only-Klassifikation, Redirect-Fo… | deliberate: one answer to 'is this stage read-only', not two | the coupling is deliberate and the alternative is worse: a SECOND answer to 'is this stage read-only' is the drift this repository has paid for repea… |
| H44 | BUG-0136 | low | TSK-0062 (`BUG-0040`) hat die Kriterien freigegebener Änderungsanträge in das Dispatch-Universum ihrer Wurzel geholt — als Ableitung über vier Terme (`dispatch._amendment_criteria_locked`), nicht als… | four measured limits of the amendment derivation, no chain | vier gemessene Grenzen der Amendment-Ableitung aus TSK-0062, je im Eintrag: die Kriterien eines ANGEWENDETEN Änderungsantrags zählen nicht mehr (Über… |
| H48 | BUG-0140 | medium | Seit TSK-0071 rendert der Kernel bei jeder Index-Regeneration auch `generated/board.html` (`kernel/state.py`, `_write_board`). Der Schreibweg ist atomar (`os.replace`), und auf Windows scheitert `os.… | deliberate fail-soft: the display freezes, the state write succeeds | keine Angriffskette auf Zustand oder Durchsetzung: eingefroren ist nur die Anzeige. An die Stelle des Schutzes tritt die bedingte Frischezusage an al… |
| H51 | BUG-0143 | medium | `gate_dispatch` verweigert das Zugende höchstens **einmal je Befund** (`mark_idle_reported` vergleicht die gespeicherte Begründung; bei `stop_hook_active` hält es still) — ein verweigerter Stop wird… | at most one refusal per finding, or it loops | die Verweigerung des Zugendes kommt höchstens einmal je Befund; danach tragen `idle_reported` auf dem Item, die Board-Karte und der Verfassungstext —… |
| H52 | BUG-0144 | medium | Ein `SubagentStop` ohne `agent_id` wird nur zugeordnet, wenn **genau ein** Dispatch der Rolle als Eigentümer in Frage kommt (`_dispatches_a_stop_could_belong_to`; gebundene, ungebundene und fristüber… | refuse rather than guess an owner | ohne `agent_id` wird verweigert statt geraten; ein Zombie-Dispatch hält damit rollengleiche id-lose Zuordnungen dauerhaft still. Begrenzung: der repo… |
| H54 | BUG-0146 | medium | Der Waisen-Term meldet eine Lease, die nie ein Kind gebunden hat und deren Fenster ablief. Ein Kind, das gestartet, aber nie gebunden wurde (Antwortform ohne `agentId`), läuft dann noch — und der Bef… | the finding is record-true; gate layer 3 refuses the unbound child anyway | ein ungebunden LAUFENDES Kind wird nach Fensterablauf als „nichts hat diesen Lauf je verfolgt" gemeldet — die Aussage ist datensatz-wahr und die Meld… |
| H55 | BUG-0147 | medium | Im Alt-Bestand (bis Kit 2026.08.14-9) existiert die Freigabeart `kit_update` nicht — es gibt nichts zu minten, und kein Hook dort kann erzwingen, dass der PM den Nutzer fragt. Die Brücke (`~/agents-a… | old stock before kit 2026.08.14-9 has no approval kind to mint | die Update-Brücke im Alt-Bestand läuft auf ein gesprochenes Ja statt einer geminteten Freigabe, und auch ein SUBAGENT erreicht sie (Alt-Gate: bridge… |
| H56 | BUG-0148 | medium | Die Brücke fährt den Installer des gestagten Kits und nimmt bei einem Kill nichts zurück; der Scaffold rollt nur zurück, wenn er selbst verweigert. | a killed run is not undone; the second run lifts cleanly | ein mitten im Kopieren abgebrochener Brückenlauf lässt ein gemischtes Bündel ohne Rücknahme stehen (kernel_files 17→22, Stempel alt); der zweite Lauf… |
| H58 | BUG-0150 | medium | `state.CONFIRMING_EVIDENCE = {"BUG": "test"}` — für TSK ist die bestätigende Kante ungewacht, obwohl Verfassung §4/Phase 9 und die QE-SKILL das Verdikt versprechen. `transition TSK-nnnn VALIDATED` lä… | a rule on a never-walked edge, and it would break the V1 import | die Kante `TSK DONE → VALIDATED` fordert keine Evidence (`state.CONFIRMING_EVIDENCE` kennt nur BUG); die Einzeiler-Regel dort säße auf einer in drei… |

```
PYTHONPATH=team-kits python -B -m kernel.cli --root project_memory \
  request-approval hole_exception --batch BUG-0106 BUG-0107 BUG-0136 BUG-0140 BUG-0143 BUG-0144 BUG-0146 BUG-0147 BUG-0148 BUG-0150
```

*built: 10 records*

### batch DESIGN-2 (10)

| hole | bug | sev | mechanism | why not closed here | what bounds it today |
|---|---|---|---|---|---|
| H59 | BUG-0151 | medium | Die evidence-Schublade hat zwei Forderer, und beide sitzen hinter Stellen, die ein kleines Projekt nie erreicht: `gate_git` fordert beim MERGE (ein Solo-Projekt auf `main` merged nie), und die `DONE… | nothing drives a solo project into phases 6-9; the emptiness is said | nichts treibt ein Projekt in die Phasen 6–9 — der Merge ist der einzige gebaute Forderer der evidence-Schublade, und ein Solo-Projekt auf `main` merg… |
| H60 | BUG-0152 | medium | Die neue Ableitung (Interview-Richtungen → `business_profile.document_sources` → Deckungsvergleich gegen die Planregeln) hat keinen erzwingenden Leser: kein Gate liest die Liste, und ein nicht begang… | an empty plan refuses the first document anyway | `document_sources` erzwingt nichts: kein Gate liest die Liste, ein nicht begangenes Interview sieht aus wie „nichts zu melden" — aber ein leerer Plan… |
| H61 | BUG-0153 | medium | Weder das ≈600-s-Default-Fenster noch ein registriertes `timeout` wird von irgendeinem Kit-Hook zur Laufzeit bemerkt — die ganze TSK-0082-Konstruktion ruht darauf, dass jede Gate-Laufzeit weit unter… | porting _harness.Deadline into the kit hooks is a build, not a fix | kein Kit-Hook merkt selbst, dass sein Fenster abläuft — jede Grenze in einem Kit-Hook ist ein Versprechen des Hooks, keine Durchsetzung; die Konstruk… |
| H67 | BUG-0159 | medium | Die Prüfung, ob eine Zeile einen **Zwilling** des Validators oder einen Köderpfad benutzt, wird im Ledger-Gate nur dann gestellt, wenn dieselbe Zeile ohnehin eine blockierte Operation trägt (ein Comm… | the decoy question is only asked where the line already blocks | die Köder- und Geschwisterprüfung des Ledger-Gates wird **nur befragt, wenn dieselbe Zeile eine blockierte Operation trägt**. Ein Lauf eines unbewach… |
| H72 | BUG-0164 | low | **Anlass:** `FR-0035` verlangte den Vier-Augen-Mechanismus wörtlich („ein Hook der das Verschieben sperrt …"), `TSK-0087` hat ihn gebaut: kein Eintritt ins Archiv ohne zwei unabhängige, an die **Byte… | measured limits of the four-eyes wall; tightening is a user decision | gemessene Grenzen der Vier-Augen-Wand, je im Eintrag: die Aufräum-Ausnahme vergleicht Vorlage+Dateiname statt Ort (ein Platzhalterwechsel läuft ohne… |
| H75 | BUG-0167 | low | **Anlass:** `BUG-0072`, Live-Fund im echten Projekt des Nutzers — der Kit-Extraktor las aus einer ZUGFeRD/CII-Rechnung den Positionswert statt der Dokumentsumme (14,28 statt 214,20) und gab das still… | the money guard is arithmetic, not semantics (FR-0065 carries the second reading) | gemessene Grenzen des E-Rechnungs-Geldpfads, je im Eintrag: der Wächter ist Arithmetik, nicht Semantik (`FR-0065` trägt die zweite Lesung); BR-CO-14… |
| H76 | BUG-0168 | low | **Anlass:** `BUG-0070/0071/0074` + `FR-0066` — vier Live-Funde in zwei Tagen: der Nutzer tippte dreimal YAML von Hand und verlor einmal drei Wireframes an ein Einfrieren, das den ganzen Vorschlagsber… | measured limits of the document write route | gemessene Grenzen des neuen Dokument-Schreibwegs, je im Eintrag: der Listeneintrag-Prosakanal (einziger Kanal ohne Wortlaut in der Karte, dreifach be… |
| H82 | BUG-0174 | low | **Anlass:** `BUG-0076`, live im echten Projekt des Nutzers (Canyon, 2026-08-30, Kit 2026.08.24-7): der PM legte dem Nutzer zweimal eine Design-Fassung vor, die niemand je gerendert hatte. Beide Runde… | seven named limits of the design sighting loop, each with the measured price of its alternative | sieben benannte Grenzen der internen Sicht-Schleife für Design-Entwürfe, je mit gemessenem Preis der Alternative; der Kern — ein gestagter Entwurf er… |
| H84 | BUG-0176 | low | **Anlass:** `FR-0071`, Nutzerfrage „wie entscheidet der PM wann welche eingesetzt werden? Nicht dass er blauäugig immer dieselben nutzt?". Gebaut: `kernel/references.py` liest die `reference_for:`-Er… | the reference derivation informs, it does not grant | die Ableitung der Referenz-Skills erzwingt nichts: der Fehler ist vorhergesagt und nicht gemessen, der Dispatch-Header gewährt nichts, die Fluchttür… |
| H88 | BUG-0180 | low | **Anlass:** `FR-0041`/N8, zweite Hälfte. Der Installer schrieb seine Sicherungen seit jeher nach `.claude/backups/<stempel>`, aber kein Befehl spielte sie zurück, und die Sicherungsliste und die Wied… | older backups carry no RESTORE_SET and are refused with a reason | der Rollback ist byte-gleich nur über die aufgezeichnete Menge; ältere Sicherungen tragen keine und werden mit Grund verweigert statt geraten, und di… |

```
PYTHONPATH=team-kits python -B -m kernel.cli --root project_memory \
  request-approval hole_exception --batch BUG-0151 BUG-0152 BUG-0153 BUG-0159 BUG-0164 BUG-0167 BUG-0168 BUG-0174 BUG-0176 BUG-0180
```

*built: 10 records*

### batch DESIGN-3 (10)

| hole | bug | sev | mechanism | why not closed here | what bounds it today |
|---|---|---|---|---|---|
| H90 | BUG-0182 | low | `_bookings` verbindet Lesungen und Zeilen über die Spalte `source` (den Beleg) und prüft dann Feld für Feld auf Übereinstimmung. Die Spalte `id` ist bewusst ausgenommen (sie entsteht erst beim Schrei… | belongs in the validator's duplicate rule, not in this gate | zwei identische Buchungen EINES Belegs teilen sich ein Lesepaar, solange keine `invoice_no` gesetzt ist; mit ihr greift `ledger_add.validate_cross`.… |
| H91 | BUG-0183 | low | `scripts/filing_plan.py --tree` und der Ablage-Abschnitt der Verfahrensdokumentation rendern die `path_template`s der Regeln. Das ist gewollt — der Baum IST der Plan, so hat der Nutzer es gesteuert —… | the tree IS the plan; plan-vs-disk is the auditor's check | der gerenderte Aktenplan-Baum zeigt den PLAN und nicht die Platte — er beschreibt die Form mit `<year>`-Platzhaltern. Der Abgleich Plan↔Platte ist di… |
| H106 | BUG-0190 | low | Das Maß aus DEC-0050 — betroffene Tests, solange die Runde offen ist; der volle Lauf EINMAL vor dem Urteil — steht seit dieser Runde dort, wo die Verdict-Rolle es liest: in der `description` ihres Fr… | no field and no hook counts runs; the role text carries it | der Umfang eines QS-Laufs ist Prosa: kein Feld im Evidence-Datensatz und kein Hook zählt, ob die Suite einmal oder zehnmal lief. Begrenzt durch den R… |
| H107 | BUG-0191 | low | Der Brief ist seit dieser Runde ein Decision-Item mit zwei Hälften — die aus dem Repo abgeleitete (Stack, Palette, Typografie, Produktvokabular, bestehende Site) und die gefragte (Ambition — bei Expl… | a decision item is free text | der Design-Brief trennt ZIEL von SCHREIBWEISE nur als Prosa: eine Prozessregel, die in den Brief gerät, fängt nichts, weil das Decision-Item Freitext… |
| H108 | BUG-0192 | medium | `EVD` trägt seit dieser Runde ein Laufprotokoll (`run_command` + `run_scope`, `backlog_types.RUN_SCOPES`), und `report._delivery_evidence` lässt einen PASS, der sich als `selection` erklärt, nicht me… | an explanation cannot be forced onto an immutable type after the fact (DEC-0061) | eine Evidenz, die GAR KEINEN Laufumfang erklärt, zählt weiter wie bisher — die Erklärungspflicht lässt sich auf einem unveränderlichen Typ nicht nach… |
| H110 | BUG-0194 | medium | Die Auflösung oben parst die Datei, die `check.ref` nennt. Für eine Datei, die kein Python ist — `src/rules.test.ts`, eine Go- oder Rust-Datei —, kann sie die Frage nicht beantworten. Sie gibt darum… | fail-closed here forbids every non-Python project a merge | einen Check, dessen Datei der Kernel nicht parsen kann (ein Testfile in einer anderen Sprache), beantwortet er mit UNENTSCHIEDEN: eine Warnung, kein… |
| H113 | BUG-0197 | low | Das Register leitet ab, was GESCHULDET ist. Ob es GETAN wurde, hält im Kit nichts fest: es gibt keinen Datensatz „diese Voranmeldung ist abgegeben", keinen „dieses Aufbewahrungsjahr ist geprüft" und… | a 'done' would be canonical state, so kernel -- over-reporting, never silence | das Fristenregister kennt kein „erledigt": nichts im Kit hält fest, dass eine Voranmeldung abgegeben oder ein Aufbewahrungsjahr geprüft wurde, also s… |
| H117 | BUG-0201 | low | Der Ledger hat keinen Kernel-Schreiber. Seine beiden Schreibwege — `scripts/ledger_add.py` und die erlaubte Handbearbeitung — kommen an genau einer Stelle vorbei, `gate_ledger_valid.handle_post_tool_… | the ledger has no kernel writer, so nothing can start the generator | nichts startet den Generator nach einer Buchung; der Datenstand im Kopf trägt das **nicht**, weil er nur das jüngste Ledgerdatum ist (ein Nachtrag lä… |
| H118 | BUG-0202 | low | Der Ledger kennt kein Fälligkeitsdatum und `business_profile.yaml` keine Zahlungsfrist (gemessen an den ausgelieferten Vorlagen). Die Seite ist außerdem eine reine Funktion der Daten — sie trägt bewu… | the page is a pure function of the data and says so | die Seite sagt beides selbst: „gerechnet beim Öffnen der Seite aus der Uhr dieses Rechners" und woher die 30 Tage kommen (§ 286 Abs. 3 BGB); ohne Skr… |
| H119 | BUG-0203 | low | Zwei Dinge, die aus derselben Wurzel kommen — es gibt nichts, was eine erzeugte Seite von einer geschriebenen unterscheidet: | DEC-0056: no hardening target | zwei Folgen einer Wurzel: (1) `_BLOCKED_SCRIPT_RX` kennt den Generator nicht, er läuft gegen einen ungültigen Ledger — begrenzt dadurch, dass er selb… |

```
PYTHONPATH=team-kits python -B -m kernel.cli --root project_memory \
  request-approval hole_exception --batch BUG-0182 BUG-0183 BUG-0190 BUG-0191 BUG-0192 BUG-0194 BUG-0197 BUG-0201 BUG-0202 BUG-0203
```

*built: 10 records*

### batch DESIGN-4 (10)

| hole | bug | sev | mechanism | why not closed here | what bounds it today |
|---|---|---|---|---|---|
| H124 | BUG-0208 | low | `_duties.briefing` nimmt `datetime.date.today()` der lokalen Maschine, einmal je Sitzungsstart, und vergleicht jede Frist gegen diesen einen Wert. Die Zeitzone des Geschäfts steht in keinem Feld des… | the business timezone is in no field of the kit | die Fristenmeldung liest die Uhr der lokalen Maschine einmal je Sitzungsstart (`_duties.briefing`, `datetime.date.today()`): eine Sitzung über Mitter… |
| H126 | BUG-0210 | low | Seit FR-0075 beantwortet die Tafel „was wartet auf dich?" — dafür muss sie `approvals/pending/` lesen und die Regel anwenden, die eine abgelaufene Anfrage nicht mehr als offen zählt. Dieselbe Regel s… | the import graph forbids the third reader calling the first | die Ablaufregel für eine offene Freigabe-Anfrage steht DREIMAL: in `approvals.open_requests` (je Datei über `pending_request`), in `report.generate_s… |
| H127 | BUG-0211 | medium | `kernel/plan_diagram.py` erzeugt `plan.drawio.svg` und `mindmap.drawio.svg` als reine Funktion der Items und legt auf die Wurzel ein `data-source-digest`, mit dem `plan_diagram.is_pristine` drei Fäll… | generated/ is ignored in every kit; the next write overwrites | eine Handänderung an einem erzeugten Diagramm sieht zwischen zwei Zustandsschreibvorgängen niemand: `plan_diagram.is_pristine` unterscheidet `pristin… |
| H132 | BUG-0215 | medium | Bis zu dieser Runde war jede item-gebundene Freigabe an GENAU EIN Item gebunden: `approvals.assert_apr_in_force` verglich `apr["item"]` mit der Id des Vorgangs, und ein `scope`-Ja konnte nichts ander… | the plan approval breaks the one-item binding on purpose (FR-0074) | die Plan-Freigabe (`approvals.PLAN_KIND`) bricht absichtlich die Ein-Item-Bindung: EINE Antwort lässt jedes Ziel ihrer Liste die Kante `DRAFT -> APPR… |
| H133 | BUG-0216 | medium | `approvals._assert_minting_caller` kannte eine Route und drei Bedingungen: ein geladenes `_kernel`-Brückenmodul, der unmittelbare Aufrufer IST die Haken-Datei, und `__main__` ist dieselbe Datei. Die… | the embedding program IS __main__ -- the third condition cannot hold on the SDK route | die zweite Mint-Route (`kernel/sdk_approval.py`, der Einstieg aus dem Agent-SDK-`canUseTool`) kann die dritte Bedingung von `approvals._assert_mintin… |
| H134 | BUG-0217 | medium | `backlog_types.EVIDENCE_RESULTS` kennt seit dieser Runde einen dritten Ausgang. Der Kernel verlangt zu einem `blocked` den Satz, der sagt, WAS den Lauf verhindert hat (`state.capture_preflight`), und… | a value cannot measure its own truth | `blocked` ist ein Zustand und keine Messung: niemand prüft nach, ob der Browser wirklich fehlte. Gemessen am Piloten mit dem ausgelieferten `gate_git… |
| H139 | BUG-0222 | medium | `FR-0077` verlangt die mechanisch prüfbaren Hälften; die Synthese verortet C1/C2/C3 im schon gebooteten `browser_smoke()` und B3 (Farbliterale im Anwendungscode) auf dem Walker `kit_checks._frontend_… | building the BUILD half of FR-0077 is a feature; PR-0012 builds no FR | die BUILD-Hälfte von `FR-0077` (C1/C2/C3 im `browser_smoke()`, B3 über `kit_checks._frontend_sources()`) ist nicht gebaut: beide Wirtsdateien liefern… |
| H140 | BUG-0223 | low | Zwei Grenzen desselben Bauprinzips („lies den Teil, der läuft"), beide in der stillen Richtung: | the rule judges what the draft DECLARES; contrast over an image is not decidable | die Rangfolge-Regel urteilt über `[data-view]`-Container, also über das, was der Entwurf DEKLARIERT: dasselbe Markup mit zwei `data-primary-action` i… |
| H142 | BUG-0225 | low | Die Werkstatt-Kommandozeile trennt seit dieser Nacharbeit zwei Klassen: was der Aufrufer GENANNT hat, muss auflösen (rc 1), was niemand genannt hat, ist kein Fehler (rc 0). Die Trennung ist nötig, we… | stricter breaks the argument-free run outside a project | die Überlappungsprüfung unterscheidet seit dieser Nacharbeit, was der Aufrufer GENANNT hat (ein `--root`, das kein Verzeichnis ist, und eine `--only`… |
| H143 | BUG-0226 | low | Eine erklärte Naht (`--seam`) wird von der Überlappungsprüfung abgezogen, weil `DEC-0062` (5) genau dafür da ist: Dateien, die kein Strom allein besitzen kann, werden vorher benannt und in der Merge-… | the seam is the orchestrator's DECLARATION and is printed line by line | eine erklärte Naht, die die GESAMTE Ownership eines der beiden Aufträge deckt, wird seit dieser Nacharbeit verweigert (rc 1) — eine, die nur einen TE… |

```
PYTHONPATH=team-kits python -B -m kernel.cli --root project_memory \
  request-approval hole_exception --batch BUG-0208 BUG-0210 BUG-0211 BUG-0215 BUG-0216 BUG-0217 BUG-0222 BUG-0223 BUG-0225 BUG-0226
```

*built: 10 records*

### batch DESIGN-5 (10)

| hole | bug | sev | mechanism | why not closed here | what bounds it today |
|---|---|---|---|---|---|
| H148 | BUG-0231 | medium | `kernel.scopes` subtrahiert die deklarierte Naht, bevor es ein Paar beurteilt, und verweigert seit der Nacharbeit eine Deklaration, die einem der beiden Aufträge **keine** eigene Ownership übrig läss… | the obvious rule would forbid the legitimate glob seam team-kits/*/VERSION | eine deklarierte Naht darf breiter sein als das, was zwei Aufträge wirklich teilen. `kernel.scopes.owns_anything_outside` verweigert nur den Totalfal… |
| H155 | BUG-0237 | medium | FR-0085 wollte die SR-Pflicht an der `class` eines Wurzelziels aufhaengen („small: kein SR; normal/large: mindestens ein ACCEPTED SR"). Gemessen, bevor sie gebaut wurde: `class` hat in diesem Kernel… | a closed vocabulary for `class` is a contract change with a migration | das Feld `class` eines Wurzelziels hat in diesem Kernel KEINE Vokabel: kein Schema, kein Validator und keine Erfassung schraenkt den Wert ein, und di… |
| H156 | BUG-0238 | medium | `dispatch._assert_no_running_lease_owns_the_same_file_locked` vergleicht den neuen Auftrag gegen die Auftraege, die eine noch nicht abgelaufene Lease halten. Zwei READY- Auftraege mit demselben `allo… | check-scopes covers the open orders; the lease sees running ones | die Dispatch-Verweigerung bei ueberlappenden Bereichen vergleicht den neuen Auftrag nur gegen Leases, die NOCH LAUFEN. Zwei READY-Auftraege mit demse… |
| H157 | BUG-0239 | medium | Die zwei Lesungen, die diese Runde vor den Auftrag setzt — der kleinere Plan und die fünf Formen, in denen eine Auftragszeile danebengeht — sind Pflichten in einem Rollentext. Was sie prüfen, ist der… | a gate over order prose falls wrong in both directions (FR-0010) | kein gebauter Leser urteilt über den WORTLAUT einer Auftragszeile: ein `TSK`, dessen `expected_outputs` einen Baustein nennt, den kein Abnahmekriteri… |
| H158 | BUG-0240 | medium | FR-0084 verlangt eine Rückschau, die an EREIGNISSEN hängt — eine Phase ist zu Ende, etwas wurde gemergt oder ausgeliefert, eine Befundklasse wiederholt sich, die Prämisse einer Entscheidung hat sich… | an occasion-driven retrospective is a build, not a fix | die Rückschau hängt an Anlässen, gebaut ist ein Takt: `_routine.routine_duties` antwortet allein aus Datum und Laufprotokoll. Gemessen je Kit über zw… |
| H160 | BUG-0242 | medium | Entfernt eine Kit-Version den Schlüssel `memory:` aus einer Rollendefinition, bleibt der Baum liegen, den eine ältere Version unter `.claude/agent-memory/<rolle>/` geschrieben hat. Bis zu dieser Rund… | the content is the user's craft knowledge (DEC-0056 (c)) | ein Kit-Update, das `memory:` aus einer Rolle entfernt, laesst den Gedaechtnisbaum liegen, den eine aeltere Version unter `.claude/agent-memory/<roll… |
| H166 | BUG-0248 | medium | TSK-0132 (PR-0009 AC-2), measured 2026-09-05 on the shipped tree: team-kits/office-team/templates/repo/scripts/invoice_intake.py::vat_of reads the document's tax breakdown (BG-23) and refuses with 't… | one ledger row carries one VAT rate; the refusal is correct and named in the contract | Until closed, the intake REFUSES rather than books: exit 2 with the rates named, nothing staged, nothing booked (tools/test_office_package.py measure… |
| H170 | BUG-0252 | low | `kernel.dispatch.ladder_for_order` reads the root's `class` and switches the effort pair to its `large` value when it is `large` (`dispatch.LARGE_CLASS`). `backlog_types.REQUIRED_FIELDS['PROC']` is `… | a PROC root carries no `class` field | the default (`medium`) is the value DEC-0078 named for ordinary office work, so nothing runs too high; the office declaration and constitution state… |
| H171 | BUG-0253 | low | `kernel.dispatch.kit_installation` answers None when `.claude/team_kit_roles.txt` does not exist at all; `ladder_for_order` then writes `{"absent": ...}` on the lease, no rung, no effort, and `spawn_… | DEC-0078 (4) forbids a default ladder without a kit declaration | the harness roles' pins are the user's decision for generation 5 (DEC-0077 (4), DEC-0080 (8)) and stand in the role files; the lease says `absent` ra… |
| H177 | BUG-0259 | low | Measured 2026-09-05 in TSK-0132 while making `reminders` ship empty: `tools/test_kit_neutrality.py::test_every_office_state_template_ships_its_lists_empty` walks the shipped office templates through… | three defensible defaults, a human in the loop by construction | What limits it today: the three values are the least loaded terms of the document and each is a defensible German-business default, none of them is a… |

```
PYTHONPATH=team-kits python -B -m kernel.cli --root project_memory \
  request-approval hole_exception --batch BUG-0231 BUG-0237 BUG-0238 BUG-0239 BUG-0240 BUG-0242 BUG-0248 BUG-0252 BUG-0253 BUG-0259
```

*built: 10 records*

### batch DESIGN-6 (3)

| hole | bug | sev | mechanism | why not closed here | what bounds it today |
|---|---|---|---|---|---|
| H178 | BUG-0260 | low | Before this round the model escalation was USER-GATED and the dev/research constitutions gave QA a way out of it: 'QA may classify a fail as `narrow-mechanical` instead; silently ignoring `escalation… | the climb counts FAILED RUNS; the miss direction is a more expensive model | The climb is bounded and cheap in the wrong direction: it goes UP one rung per failed run and is capped at the role's top (`ladder_for_order`, measur… |
| H179 | BUG-0261 | medium | MECHANISM: PR-0008 AC-1 asks for one of three verdicts per active BUG, 'measured against the running code (its observed line re-run or its test executed)'. The cheap measurement is the second one: ru… | a run outcome is not a verdict -- the user's click is (DEC-0100) | What takes the place of it: nothing invents a verdict. Every row of the table carries its measured line and its verdict WORD is the measurement, not… |
| H182 | BUG-0264 | medium | PR-0010 AC-1 asks that no watcher definition or README sentence claim a schedule the repo does not build. `tools/test_radar_trigger.py` reads exactly three texts -- `radar/README.md` and the two role… | .claude/hooks/gate_spawn_needs_item.py + _harness.py -- forbidden_scope, PATCH for the user | The four sentences GRANT nothing and refuse nothing: `gate_spawn_needs_item` decides the exemption on the frontmatter key `harness_item:` in the role… |

```
PYTHONPATH=team-kits python -B -m kernel.cli --root project_memory \
  request-approval hole_exception --batch BUG-0260 BUG-0261 BUG-0264
```

*built: 3 records*

## The two DEFECTS on the same route (BUG-0055, BUG-0056)

Both are measured unclosable and both carry a bound, and NEITHER is a numbered hole -- which is why `hole_exception_batch` keys its refusal on the BOUND and not on `hole_number`. What they still lack is the `limits` line itself; nothing but the kernel may write it, and `project_memory/**` is this order's forbidden scope. The lead writes it and then asks:

* **BUG-0055** -- `The scope manifest has no wireframe field -- a re-frozen wireframe invalidates nothing, and nothing can check ` (today: no `limits` field)
* **BUG-0056** -- `A recorded V1 file outside the state tree is writable -- protected is the PLACE, not the record` (today: no `limits` field)

**BUG-0055** -- the bound, as the lead writes it:

```
PYTHONPATH=team-kits python -B -m kernel.cli --root project_memory update BUG-0055 \
  < project_memory/staging/TSK-0140/bug-0055-limits.json
```

**BUG-0056** -- the bound, as the lead writes it:

```
PYTHONPATH=team-kits python -B -m kernel.cli --root project_memory update BUG-0056 \
  < project_memory/staging/TSK-0140/bug-0056-limits.json
```

Then the question over both:

```
PYTHONPATH=team-kits python -B -m kernel.cli --root project_memory \
  request-approval hole_exception --batch BUG-0055 BUG-0056
```

**Totals: 105 gaps in 13 batches, plus one batch of two defects = 14 questions.**
