{
  "observed": "MEASURED 2026-09-06 (TSK-0133, generation-5 merge), each line as a Bash tool call in the main checkout, so the hook itself judged it:\n  * `cd \"C:/Offline Repos/AgentAndSkills\" && grep -c \"\\`a\\`, \\`b\\`\" README.md` -- REFUSED by `.claude/hooks/gate_lead_write_scope.py`: 'no tool call in this repo may write C:\\. this path is refused for what stands UNDER it ... carried with its measured chain as H19'. The same refusal hit the merge twice before on longer lines whose grep pattern had the same shape (`\"\\`dispatch\\`, \\`ladder\\`, \\`submit-result\\`.*\\`migrate-holes\\`, \\`sweep-pointers\\`\"`); one of those lines also carried the `git add` that staged five resolved conflict files, and the add did not run -- noticed only because the next check found the files unstaged.\n  * Three neighbours PASS THE GATE (the hook let them run; what grep then returned is grep's own exit code -- `grep -c` exits 1 when the count is 0, which two of them were, corrected after verify round 1, P2): `grep -c \"\\`ladder\\`\" README.md` (ran, printed 1, rc 0), `grep -c \"\\`a\\`.*\\`b\\`\" README.md` (ran, printed 0, rc 1 -- a wildcard but no space), `grep -c \"a.*b, c\" README.md` (ran, printed 0, rc 1 -- a space but no backticks).\n  * So the trigger is a double-quoted word that carries BOTH an escaped backtick (a literal backtick to bash: inside double quotes `\\`` is not a command substitution) and a space. Which piece of that word the reader resolves to `C:\\` was NOT isolated: the reader is `_harness` in `.claude/hooks/`, forbidden to this round, and the refusal text names the drive root and not the word it read.",
  "repro": "In a Bash tool call of this repo: `cd \"C:/Offline Repos/AgentAndSkills\" && grep -c \"\\`a\\`, \\`b\\`\" README.md` -> hook error rc 2 naming `C:\\`, grep never runs. Control: the same line with `\"\\`a\\`.*\\`b\\`\"` or with `\"a.*b, c\"` -> the hook lets it run (grep prints 0 and exits 1, its own answer for no match)."
}
