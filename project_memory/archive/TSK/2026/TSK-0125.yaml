id: TSK-0125
product_requirement: PR-0007
derives_from: PR-0007
type: ops
assigned_role: harness-implementer
acceptance_refs:
- AC-1
- AC-2
- AC-3
- AC-4
allowed_scope:
- .github/**
- .gitattributes
- team-kits/kernel/kitupdate.py
- .claude/hooks/test_gates.py
- team-kits/*/VERSION
- tools/**
- docs/**
forbidden_scope:
- team-kits/kernel/backlog_types.py
- team-kits/kernel/state.py
- team-kits/kernel/approvals.py
- team-kits/kernel/dispatch.py
- team-kits/kernel/report.py
- team-kits/kernel/scopes.py
- team-kits/kernel/cli.py
- team-kits/kernel/board.py
- team-kits/kernel/filing.py
- team-kits/*/hooks/**
- team-kits/*/settings/**
- team-kits/*/constitution/**
- team-kits/*/agents/**
- team-kits/*/skills/**
- team-kits/*/templates/**
- .claude/hooks/gate_*.py
- .claude/hooks/_harness.py
- .claude/settings.json
- .claude/agents/**
- scaffold_team.*
- install.*
- init_project_memory.*
- team-kits/gen_provider_artifacts.py
- user/**
- CLAUDE.md
- project_memory/**
required_inputs:
- 'project_memory/product/active/PR-0007.yaml (AC-1..AC-4) and what it absorbed: project_memory/bugs/active/BUG-0069.yaml
  (CI red on every push; the three failure classes named there; TSK-0088 cancelled
  in gen 3), BUG-0025.yaml (.gitattributes eol; 33 CRLF files measured in generation
  3, cause core.autocrlf=true local and system), BUG-0088.yaml (kit update leaves
  the agent-memory tree), BUG-0033.yaml (gate-3 timing test red under load, green
  solo -- five times in generation 3)'
- DEC-0070 (the (g) notes on BUG-0033 and the CRLF incident), DEC-0056
- .github/workflows/** (the hosted runs), .gitattributes, tools/validate.py (the size/eol
  reader), team-kits/kernel/kitupdate.py, .claude/hooks/test_gates.py::test_gate3_answers_before_its_registration_however_costly_the_line_is_to_judge
  and its budget derivation, .claude/settings.json timeouts (READ ONLY)
- 'project_memory/staging/TSK-0120/merge-protocol.md section 1 (eol measurement: 35
  -> 33 CRLF files, two normalised with byte-equality against HEAD) and the C stream''s
  byte sweep (binaries decided by bytes, 2 071 binaries contain \r\n)'
expected_outputs:
- 'PR-0007 AC-1 (BUG-0069): the workflow is green on ubuntu and windows, or every
  red is a named skip with its class (shallow clone history, D: workspace mount, fixture
  size) -- never a failure the local suite does not reproduce; measured on one real
  hosted run (the user pushes on request; you prepare and measure locally what you
  can, and name what only the hosted run shows).'
- 'PR-0007 AC-2 (BUG-0025): .gitattributes pins every text class; a shipped test /
  validate.py refuses a tracked text file with CRLF and names it; the 33 files normalised
  ONCE, binaries decided by bytes not extension, byte-equality against HEAD as the
  precondition; core.autocrlf named in the developer docs as the cause, not fixed
  by this repo.'
- 'PR-0007 AC-3 (BUG-0088): update-kit removes the agent-memory tree the previous
  kit owned or lists it for the user with a sentence; measured on a pilot; red-first.'
- 'PR-0007 AC-4 (BUG-0033): the gate-3 timing test measures against a budget DERIVED
  from the registered timeout with a load-aware margin, or names the load class it
  is -- no bare assert against a seconds constant; measured solo AND under a load
  class this host tolerates: the load rig runs ONLY in the window the lead names after
  every other generation-4 stream has reported, never beside another stream''s suite,
  with at most HALF of the host''s logical CPUs as burners, at below-normal priority,
  inside a window of at most 120 s -- the 16-burner rig preceded two of the four hard
  power-offs of 2026-09-04 by 2-3 min (Windows Kernel-Power 41 at 17:38, 22:58, 23:06,
  23:14; measured in staging/generation-4-streams.md). Until that window the rig is
  REDESIGNED and its design (burner count, priority, cap, the kill path) stands in
  the protocol; it does not run.'
- Per absorbed wish or bug its own red-first test measured in a copy outside the repo
  (restore the defect, see red, put it back) and its own acceptance line in the protocol;
  the verifier reports PER acceptance criterion of the goal. Every new property claim
  -- code comment, docstring, docs, hole list -- is MEASURED before handover; a named
  test must be able to fail (four cases in generation 3 could not); every hole-list
  citation carries its module prefix; hole numbers H160 to H162 ONLY, reserved here
  in the item; forbidden_scope project_memory/** EXCEPTS project_memory/staging/(this
  task id)/ -- said here so the item does not contradict itself (generation-3 cut
  finding). Wherever the truth about a shell can be executed, a real shell is the
  arbiter, not a derivation (DEC-0070). Only the READING suites run in the stream;
  the full suite belongs to the merge (DEC-0050, five breaches in generation 3 ~5
  h).
- 'Seams: .claude/hooks/test_gates.py shared with G4-1 and G4-2 -- your change is
  ONE test, named; kitupdate.py is yours and forbidden to G4-2 (say so in the protocol);
  no constitution sentence expected.'
- 'Handover: worktree C:/Offline Repos/v2-testbed/_worktrees/g4-hygiene (branch g4/hygiene
  off feat/harness-v2 at 75a00d1); ALL scratch only under C:/Offline Repos/v2-testbed/_round-scratch/(this
  task id)/; a red-first rig refuses to run outside its own directory and writes binary
  (the generation-3 CRLF incident); verifier copies made WITHOUT the .git file of
  the worktree; patch = the worktree diff WITHOUT the VERSION hunks at C:/Offline
  Repos/v2-testbed/_round-scratch/(this task id)/stream-hygiene.patch (a patch carrying
  VERSION hunks is a cut finding, DEC-0070); protocol at project_memory/staging/(this
  task id)/stream-protocol.md with: seam table (received / expected at merge), per-criterion
  acceptance line + red-first tests, measured lines, what was deliberately not closed
  but named, the one-line rejected alternative of the plan (FR-0084 shape), suite
  runs (reading suites only), provisional VERSION stamp, wall-clock and tokens for
  the (g) table. No commit, no push, no install to the global store.'
- 'Host rule (2026-09-04, four hard power-offs): no command of this stream saturates
  every logical CPU of the host, and no load measurement starts without the lead naming
  its window; a rig that burns CPU states burner count, priority and cap in the protocol
  BEFORE its first run. Everything else in this order continues now, beside the other
  three streams. Successor of TSK-0124 (CANCELLED for this re-cut; its worktree g4-hygiene,
  patch and protocol carry over under the new id).'
dependencies: []
seam_scope:
- tools/**
- docs/**
- team-kits/*/VERSION
- .claude/hooks/test_gates.py
root_revision: 1
status: CANCELLED
revision: 1
approval_ref: null
created: '2026-09-04T23:29:50'
closed_at: '2026-09-05T21:32:24'
