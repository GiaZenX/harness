id: TSK-0123
product_requirement: PR-0006
derives_from: PR-0006
type: implementation
assigned_role: harness-implementer
acceptance_refs:
- AC-1
- AC-2
- AC-3
- AC-4
allowed_scope:
- team-kits/*/constitution/**
- team-kits/*/skills/project-manager/**
- team-kits/office-team/skills/office-manager/**
- team-kits/*/skills/project-auditor/**
- team-kits/*/skills/parallel-streams/**
- team-kits/*/agents/project-auditor.md
- team-kits/*/agents/project-manager.md
- .claude/agents/harness-lead.md
- .claude/agents/harness-implementer.md
- .claude/agents/harness-verifier.md
- team-kits/*/VERSION
- tools/**
- docs/**
forbidden_scope:
- team-kits/kernel/**
- team-kits/*/hooks/**
- team-kits/*/templates/**
- team-kits/*/settings/**
- team-kits/*/skills/quality-engineer/**
- team-kits/*/skills/humanizer/**
- team-kits/dev-team/skills/frontend-design/**
- team-kits/dev-team/skills/product-designer/**
- .claude/hooks/**
- .claude/settings.json
- .github/**
- .gitattributes
- scaffold_team.*
- install.*
- init_project_memory.*
- team-kits/gen_provider_artifacts.py
- user/**
- CLAUDE.md
- project_memory/**
required_inputs:
- 'project_memory/product/active/PR-0006.yaml (AC-1..AC-4) and what it absorbed: project_memory/archive/FR/2026/FR-0084.yaml
  (review as EVENT: four measured questions, three lines to the user, the rejected-alternative
  line in every TSK plan), FR-0005.yaml (cut critic presents a SMALLER plan), FR-0010.yaml
  (the five malformed order-line forms), project_memory/decisions/active/DEC-0070.yaml
  (rules 1, 2, 5 for the harness-lead role)'
- DEC-0062, DEC-0063, DEC-0067 (the PM procedure of parallel specialists -- the parallel-streams
  skill you now own), DEC-0074-era context in FR-0074 (taste stays the user's), DEC-0056
- 'team-kits/*/constitution/AGENTS.md, skills/project-manager/SKILL.md, skills/project-auditor/SKILL.md,
  agents/project-auditor.md (the FR-0038/TSK-0112 run record: ''the hook reports,
  the PM spawns'' -- the trigger lives in hooks/_routine.py + session_status.py, which
  G4-1 owns: your trigger sentences are a SEAM to G4-1), .claude/agents/harness-lead.md
  (gate 1 refuses the lead; you edit it), the lead-in rule test for shared constitution
  paragraphs (tools/test_role_contracts.py), tools/test_shortening_net.py (section
  pins)'
- 'project_memory/staging/generation-3-streams.md: the eight cut findings and three
  orchestrator failures = the cases FR-0005/FR-0010/DEC-0070 rule 5 are written against;
  project_memory/staging/TSK-0114/merge-protocol.md section 9 and TSK-0120 section
  9 ((g) tables = the retrospective''s data)'
expected_outputs:
- 'PR-0006 AC-1 (FR-0084): the project-auditor role (kits) and the harness-lead role
  (here) ask the four measured questions at the named events (phase end, merge/release,
  repeated finding class, changed decision premise) and write three lines to the user
  into the session brief / the round log; a TSK plan without the rejected-alternative
  line is refused by the plan check (where a check exists; else the skill''s own step
  with a measured pilot); measured on a scaffolded pilot; the trigger wiring in hooks
  is a SEAM to G4-1, verbatim.'
- 'PR-0006 AC-2 (FR-0005): a cut critic step before the order in the PM skill (and
  in harness-lead.md here): it presents a smaller plan with what it removed and why,
  the PM records the choice; measured against the kit text and on a pilot.'
- 'PR-0006 AC-3 (FR-0010): the five malformed order-line forms in the project-manager
  role text as a procedure with the case behind each; measured against the kit text
  (a test that can fail).'
- 'PR-0006 AC-4 (DEC-0070): harness-lead.md carries rule 1 (measure the cut with check-scopes
  before spawn; reserve hole numbers in the item), rule 2 (a command-line guard is
  a DEC-first design round), rule 5 (a queued message is no delivery; silence longer
  than a verification''s wall-clock = ListAgents), each pointing at DEC-0070; the
  harness-implementer / -verifier texts gain the generation-3 lessons that are theirs
  (real shell as arbiter; a rig refuses to run outside its directory and writes binary;
  a named test must be able to fail); a tools test reads the role texts for the pointers.'
- Per absorbed wish or bug its own red-first test measured in a copy outside the repo
  (restore the defect, see red, put it back) and its own acceptance line in the protocol;
  the verifier reports PER acceptance criterion of the goal. Every new property claim
  -- code comment, docstring, docs, hole list -- is MEASURED before handover; a named
  test must be able to fail (four cases in generation 3 could not); every hole-list
  citation carries its module prefix; hole numbers H157 to H159 ONLY, reserved here
  in the item; forbidden_scope project_memory/** EXCEPTS project_memory/staging/(this
  task id)/ -- said here so the item does not contradict itself (generation-3 cut
  finding). Wherever the truth about a shell can be executed, a real shell is the
  arbiter, not a derivation (DEC-0070). Only the READING suites run in the stream;
  the full suite belongs to the merge (DEC-0050, five breaches in generation 3 ~5
  h).
- 'Seams: you RECEIVE sentences from G4-1 (the test-scope gate), G4-2 (SR duty, inbox
  rule, hole items, lease refusal) and G4-4 (none expected) -- table ''received at
  cut time: none; expected at merge: G4-1, G4-2'' in the protocol; the shared constitution
  paragraph rule (in >= 2 constitutions => in all three or listed with reason) measured
  by the existing tests.'
- 'Handover: worktree C:/Offline Repos/v2-testbed/_worktrees/g4-procedure (branch
  g4/procedure off feat/harness-v2 at 75a00d1); ALL scratch only under C:/Offline
  Repos/v2-testbed/_round-scratch/(this task id)/; a red-first rig refuses to run
  outside its own directory and writes binary (the generation-3 CRLF incident); verifier
  copies made WITHOUT the .git file of the worktree; patch = the worktree diff WITHOUT
  the VERSION hunks at C:/Offline Repos/v2-testbed/_round-scratch/(this task id)/stream-procedure.patch
  (a patch carrying VERSION hunks is a cut finding, DEC-0070); protocol at project_memory/staging/(this
  task id)/stream-protocol.md with: seam table (received / expected at merge), per-criterion
  acceptance line + red-first tests, measured lines, what was deliberately not closed
  but named, the one-line rejected alternative of the plan (FR-0084 shape), suite
  runs (reading suites only), provisional VERSION stamp, wall-clock and tokens for
  the (g) table. No commit, no push, no install to the global store.'
dependencies: []
seam_scope:
- tools/**
- docs/**
- team-kits/*/VERSION
root_revision: 1
status: CANCELLED
revision: 1
approval_ref: null
created: '2026-09-04T06:01:47'
closed_at: '2026-09-05T21:32:22'
