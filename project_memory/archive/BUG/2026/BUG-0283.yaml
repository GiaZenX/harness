id: BUG-0283
title: 'H12 neu aufgelegt (Ausnahme nie freigegeben): Ein Subagent kann sich die Ausnahme
  von Gate 2 selbst ausstellen'
related_pr: PR-0012
observed: 'Migrated 2026-09-05 as H12 into BUG-0104 with status ACCEPTED_EXCEPTION
  and approval null -- the user never saw a question for it (measured 2026-09-12 over
  archive/bug/*). Original finding: **Mechanismus:** Gate 2 liest die Ausnahme (`harness_item:
  none`) aus der Frontmatter der gespawnten

  Rolle, und liest sie bei **jedem** Aufruf frisch — es ist nicht der Provider, der
  eine Rolle beim

  Sitzungsstart lädt, sondern der Hook, der eine Datei öffnet. Gate 1 schützt `.claude/`
  nur gegen den

  **Sitzungsagenten**; ein Subagent muss dort schreiben dürfen, weil der Auftrag des
  Umsetzers

  regelmäßig `.claude/hooks/**` nennt und dieses Repo keinen Dispatch hat, gegen den
  sich ein

  Subagent-Scope prüfen ließe.'
expected: 'The fix lies in .claude/hooks/gate_spawn_needs_item.py (this repo''s own
  gate), forbidden to every role of this repo: the exact patch goes to the user''s
  shell patch file (h182 precedent); until applied, an exception with this sentence:
  Ein Unteragent koennte sich die Ausnahme der Startpruefung selbst eintragen; die
  Stelle liegt in einer Schutzdatei dieses Repos, die nur du von aussen aendern kannst.'
repro: 'read BUG-0104 (status ACCEPTED_EXCEPTION, approval: null); the mechanism:
  docs/holes/H12.md'
severity: medium
acceptance_criteria:
- 'AC-1: the item ends VERIFIED on a passing test that names it, or ACCEPTED_EXCEPTION
  on a request the user answered -- never on a migration'
- 'AC-2: if an exception, its `limits` is the plain-German sentence a non-developer
  understands'
source: 'archive BUG-0104; staging/generation-6/refile_unclicked_exceptions.py (lead
  classification: gesperrt, stream -)'
limits: Ein Unteragent könnte sich die Ausnahme der Startprüfung selbst eintragen;
  die Stelle liegt in einer Schutzdatei dieses Projekts, die nur du von außen ändern
  kannst.
supersedes: BUG-0104
hole_number: H199
status: ACCEPTED_EXCEPTION
revision: 1
approval_ref: APR-0062
created: '2026-09-12T09:46:18'
approved_hash: 8cb035b88d2e2af80909f44b3d11b6b91b557adffdf0e30faaddd2fcabcadddd
closed_at: '2026-09-12T22:41:50'
