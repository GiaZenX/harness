id: BUG-0281
title: 'H7 neu aufgelegt (Ausnahme nie freigegeben): `carries_work` verlangt keinen
  erreichbaren Endzustand — OFFEN'
related_pr: PR-0012
observed: 'Migrated 2026-09-05 as H7 into BUG-0099 with status ACCEPTED_EXCEPTION
  and approval null -- the user never saw a question for it (measured 2026-09-12 over
  archive/bug/*). Original finding: **Mechanismus:** Gate 4 verlangt, dass ein Eintrag
  ein Item nennt, das Arbeit tragen **kann** (hat

  einen Automaten) und **nicht terminal** ist. `SR` hat die Kette `PROPOSED → ACCEPTED`
  und als

  einzigen Endzustand `SUPERSEDED`. Das natürliche Lebensende eines `SR` ist damit
  `ACCEPTED` — und

  das ist kein Endzustand, also trägt ein angenommenes `SR` für Gate 4 für immer „offene
  Arbeit".'
expected: 'CLOSED red-first by stream A with a test that NAMES this bug id (DEC-0100);
  fix location: team-kits/kernel/backlog_types.py AUTOMATA done_states + kernel carries_work.
  If the attempt measures it unclosable here, the downgrade carries the measurement
  and a plain-German sentence (never ''later'').'
repro: 'read BUG-0099 (status ACCEPTED_EXCEPTION, approval: null); the mechanism:
  docs/holes/H7.md'
severity: medium
acceptance_criteria:
- 'AC-1: the item ends VERIFIED on a passing test that names it, or ACCEPTED_EXCEPTION
  on a request the user answered -- never on a migration'
- 'AC-2: if an exception, its `limits` is the plain-German sentence a non-developer
  understands'
source: 'archive BUG-0099; staging/generation-6/refile_unclicked_exceptions.py (lead
  classification: ja, stream A)'
limits: Gate 4 prüft Form, nicht Deckung; die Reparaturstelle liegt im Kernel (`AUTOMATA`
  ohne `done_states`)
supersedes: BUG-0099
hole_number: H197
status: OPEN
revision: 1
approval_ref: null
created: '2026-09-12T09:46:11'
