id: EVD-0419
kind: test
related:
- BUG-0153
result: pass
summary: 'H61 closed in both halves: a timeout on every entry of the three kits''
  settings.json (30/30/27 added, 2 of 89 carried one before) and the fail-closed deadline
  reader moved into _compat, which every shipped hook imports and whose load() every
  shipped hook calls -- so the 45 hooks that never import _kernel are bounded too.
  Red-first in a .git-less copy: guard_no_adhoc.py rc 0 with empty stderr under a
  1.5 s window without the arming call, rc 2 with it.'
artifact_refs:
- staging/TSK-0147/protocol.md
run_command: python -B -m pytest tools/test_hooks.py -q -k "test_a_hook_that_never_imports_the_kernel_is_bounded_by_its_window_too
  or test_every_registration_names_a_window_its_gate_can_answer_inside or test_a_registration_that_names_a_gate_without_a_window_refuses_every_call_of_it"
run_scope: selection
revision: 1
approval_ref: null
created: '2026-09-13T00:08:37'
